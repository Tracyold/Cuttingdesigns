both files scan for all different elements inside of a single file.

its prompts you for the file path

the outputs all elements that could be turned into separate component files. both can be helpful, one scripts has more constraints than the other. run both see what you get.