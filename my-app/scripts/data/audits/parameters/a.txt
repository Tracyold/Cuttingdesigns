╔══════════════════════════════════════════════════════════════════╗
║  REPORT 1 — inquiries.tsx                                        ║
║  Grouped by section comment header                               ║
╚══════════════════════════════════════════════════════════════════╝

══════════════════════════════════════════════════════════════════════
  ── Light theme token overrides
══════════════════════════════════════════════════════════════════════

  .light {
    --base: #e8ecf2
    --base-whisper: #dfe4ec
    --base-mute: #f1f4f9
    --ink: #2b3445
    --ink-whisper: #4a5363
    --ink-mute: #8893a6
    --accent: #1a82c8
    --accent-mute: #cee5f5
    --sedona: #976a63
    --divider: rgba(0,0,0,0.07)
    --neu-dark: rgba(146,158,178,0.55)
    --neu-light: rgba(255,255,255,0.95)
    --scrim: rgba(43,52,69,0.35)
    --bg-outer: #c8cdd8
  }

  * {
    box-sizing: border-box
    margin: 0
    padding: 0
  }

  body {
    background: var(--bg-outer)
    display: flex
    align-items: center
    justify-content: center
    min-height: 100vh
    font-family: var(--font)
    padding: 32px 16px
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── theme toggle buttons — preview only
══════════════════════════════════════════════════════════════════════

  .theme-row {
    position: fixed
    top: 16px
    right: 16px
    z-index: 9999
    display: flex
    gap: 8px
  }

  .tbtn {
    padding: 7px 16px
    border-radius: 999px
    border: none
    cursor: pointer
    font-size: 11px
    font-weight: 700
    font-family: var(--font)
    letter-spacing: 0.5px
  }

  .tbtn.dark {
    background: #1c2230
    color: #d4ec5a
  }

  .tbtn.lite {
    background: #e8ecf2
    color: #1a82c8
  }

══════════════════════════════════════════════════════════════════════
  ── phone shell
══════════════════════════════════════════════════════════════════════

  .phone {
    width: 390px
    height: 844px
    border-radius: 50px
    overflow: hidden
    background: var(--base)
    position: relative
    display: flex
    flex-direction: column
    box-shadow: 0 40px 80px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.06)
    transition: background 0.3s ease
  }

  .status-bar {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 14px 28px 0
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    flex-shrink: 0
    position: relative
    z-index: 5
  }

  .notch {
    width: 120px
    height: 28px
    background: var(--base-mute)
    border-radius: 0 0 18px 18px
    margin-top: -14px
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── dashboard placeholder
══════════════════════════════════════════════════════════════════════

  .home {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    padding: 24px 24px 40px
    scrollbar-width: none
  }

  .home::-webkit-scrollbar {
    display: none
  }

  .home-title {
    font-size: 58px
    font-weight: 800
    line-height: 1
    color: var(--ink)
    letter-spacing: -2px
    margin-bottom: 4px
  }

  .home-date {
    font-size: 11px
    font-weight: 600
    letter-spacing: 2px
    text-transform: uppercase
    color: var(--ink-mute)
    margin-bottom: 12px
  }

  .home-hint {
    font-size: 13px
    color: var(--ink-mute)
    margin-top: 24px
    opacity: 0.5
  }

  .bottom-nav {
    height: 100px
    background: var(--base)
    box-shadow: 0 -8px 24px var(--neu-dark)
    display: flex
    align-items: center
    justify-content: space-around
    padding: 12px 20px 24px
    flex-shrink: 0
    z-index: 20
    position: relative
    transition: background 0.3s ease
  }

  .nav-btn {
    width: 40px
    height: 40px
    border-radius: 50%
    border: none
    cursor: pointer
    background: transparent
    display: flex
    align-items: center
    justify-content: center
    font-size: 17px
    color: var(--ink-mute)
    transition: all 0.18s cubic-bezier(0.175, 0.885, 0.32, 1.275)
    -webkit-tap-highlight-color: transparent
  }

  .nav-btn:active {
    transform: scale(0.88)
  }

  .nav-btn.fab {
    width: 54px
    height: 54px
    background: var(--base)
    font-size: 20px
    box-shadow: var(--neu-sm)
    transition: background 0.3s ease, box-shadow 0.18s ease, transform 0.18s ease
  }

  .light .nav-btn.fab {
    box-shadow: 2px 2px 6px rgba(146,158,178,0.3), -2px -2px 6px rgba(255,255,255,0.8)
  }

  .nav-btn.fab.active {
    background: var(--accent)
  }

  .nav-btn.active:not(.fab) {
    color: var(--accent)
  }

══════════════════════════════════════════════════════════════════════
  ── background scrim behind panel
══════════════════════════════════════════════════════════════════════

  .panel-scrim {
    position: absolute
    top: 0
    left: 0
    right: 0
    bottom: 100px
    z-index: 15
    background: var(--scrim)
    opacity: 0
    pointer-events: none
    transition: opacity 0.38s ease
  }

  .panel-scrim.open {
    opacity: 1
    pointer-events: auto
  }

══════════════════════════════════════════════════════════════════════
  ── panel sheet
══════════════════════════════════════════════════════════════════════

  .panel {
    position: absolute
    left: 0
    right: 0
    bottom: 100px
    height: calc(100% - 48px - 100px)
    background: var(--base)
    border-radius: 28px 28px 0 0
    z-index: 18
    display: flex
    flex-direction: column
    transform: translateY(100%)
    transition: transform 0.44s cubic-bezier(0.16, 1, 0.3, 1), background 0.3s ease
    will-change: transform
    overflow: hidden
  }

  .panel.open {
    transform: translateY(0)
  }

  .panel.closing {
    transform: translateY(100%)
    transition: transform 0.30s cubic-bezier(0.4, 0, 1, 1)
  }

══════════════════════════════════════════════════════════════════════
  ── drag handle header
══════════════════════════════════════════════════════════════════════

  .panel-header {
    background: transparent
    border-radius: 28px 28px 0 0
    padding: 10px 20px 12px
    flex-shrink: 0
    position: relative
    cursor: grab
    touch-action: none
    user-select: none
  }

  .drag-pill {
    display: flex
    justify-content: center
    padding-bottom: 10px
  }

  .drag-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(--accent)
    opacity: 0.4
  }

══════════════════════════════════════════════════════════════════════
  ── swipe hint text
══════════════════════════════════════════════════════════════════════

  .panel-hint {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.6px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.45
    margin-bottom: 6px
    text-align: center
  }

══════════════════════════════════════════════════════════════════════
  ── panel title — sunken neumorphic style
══════════════════════════════════════════════════════════════════════

  .panel-title {
    font-size: 30px
    font-weight: 800
    letter-spacing: 0.5px
    line-height: 1.1
    padding-right: 50px
    padding-left: 8px
    padding-bottom: 8px
    margin-top: 16px
    color: #3d5070
    text-shadow: -1px -1px 1px rgba(6,9,15,0.9), 1px 1px 2px rgba(72,86,110,0.5)
    transition: none
  }

  .light .panel-title {
    color: #c8cdd8
    text-shadow: -1px -1px 1px rgba(146,158,178,0.6), 1px 1px 2px rgba(255,255,255,0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── title states
══════════════════════════════════════════════════════════════════════

  .panel-title.has-new {
    color: var(--accent)
    text-shadow: none
  }

  .panel-title.is-archived {
    color: var(--sedona)
    text-shadow: none
  }

══════════════════════════════════════════════════════════════════════
  ── close button (× in topright)
══════════════════════════════════════════════════════════════════════

  .panel-close-track {
    position: absolute
    top: 11px
    right: 16px
    width: 36px
    height: 36px
    border-radius: 50%
    background: var(--base)
    box-shadow: inset 2px 2px 5px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
    transition: background 0.3s ease
  }

  .panel-close {
    width: 26px
    height: 26px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(--base-whisper)
    color: var(--ink-whisper)
    opacity: 0.9
    font-size: 13px
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 3px 3px 7px var(--neu-dark), -2px -2px 5px var(--neu-light)
    transition: all 0.14s ease
  }

  .panel-close:hover {
    opacity: 1
    color: var(--accent)
  }

  .panel-close:active {
    opacity: 1
    color: var(--accent)
    box-shadow: var(--neu-in)
    transform: scale(0.95)
  }

  .tabs-wrap {
    flex-shrink: 0
    padding: 4px 32px 12px
  }

  .tabs {
    padding: 3px
    border-radius: 20px
    background: var(--base-mute)
    box-shadow: inset 2px 2px 5px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    display: flex
    position: relative
    overflow: hidden
  }

══════════════════════════════════════════════════════════════════════
  ── sliding highlight pill
══════════════════════════════════════════════════════════════════════

  .tab-highlight {
    position: absolute
    top: 3px
    bottom: 3px
    width: calc(50% - 3px)
    border-radius: 16px
    background: var(--accent)
    box-shadow: 2px 2px 5px var(--neu-dark), -1px -1px 3px var(--neu-light)
    left: 3px
    pointer-events: none
    transition: none
  }

  .tab-highlight.right {
    transform: translateX(100%)
  }

  .tab-btn {
    flex: 1
    height: 30px
    border: none
    border-radius: 16px
    cursor: pointer
    font-family: var(--font)
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    background: transparent
    position: relative
    z-index: 1
    transition: color 0.18s ease
    -webkit-tap-highlight-color: transparent
  }

  .tab-btn.active {
    color: var(--base-mute)
  }

  .light .tab-btn.active {
    color: #fff
  }

  .inq-list {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    -webkit-overflow-scrolling: touch
    min-height: 0
  }

  .inq-list::-webkit-scrollbar {
    display: none
  }

  .tab-pane {
    width: 100%
    display: none
    flex-direction: column
    flex-shrink: 0
  }

  .tab-pane.active {
    display: flex
  }

  .pane-locked {
    flex-shrink: 0
  }

  .pane-overflow {
    flex-shrink: 0
    display: none
  }

  .pane-overflow.open {
    display: block
  }

══════════════════════════════════════════════════════════════════════
  ── section label row (e.g. "top three" / recover link)
══════════════════════════════════════════════════════════════════════

  .inq-section-label {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.8px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
    padding: 12px 24px 6px
    display: flex
    align-items: center
    justify-content: space-between
  }

  .inq-item {
    padding: 16px 24px 26px
    border-bottom: 0.5px solid var(--divider)
    display: flex
    align-items: center
    cursor: pointer
    position: relative
    transition: background 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .inq-item:last-child {
    border-bottom: none
  }

  .inq-item:active {
    background: var(--base-whisper)
  }

  .inq-body {
    flex: 1
    min-width: 0
    padding-right: 8px
  }

  .inq-title {
    font-size: 18px
    font-weight: 400
    color: var(--ink)
    line-height: 1.2
    margin-bottom: 5px
    white-space: nowrap
    overflow: hidden
    text-overflow: ellipsis
  }

  .inq-meta {
    display: flex
    align-items: center
    gap: 8px
  }

  .inq-date {
    font-size: 12px
    color: var(--ink-mute)
    font-weight: 400
  }

  .inq-sep {
    font-size: 10px
    color: var(--ink-mute)
    opacity: 0.35
  }

  .inq-sender {
    font-size: 12px
    color: var(--ink-mute)
    font-weight: 500
  }

══════════════════════════════════════════════════════════════════════
  ── notification arrow — pulsed via JS when activity=hot
══════════════════════════════════════════════════════════════════════

  .inq-arrow {
    font-size: 15px
    flex-shrink: 0
    transition: none !important
  }

  .inq-arrow.dim {
    color: var(--ink-mute)
    opacity: 0.35
    animation: none
  }

══════════════════════════════════════════════════════════════════════
  ── bottomright corner: date · id
══════════════════════════════════════════════════════════════════════

  .inq-corner {
    position: absolute
    bottom: 8px
    right: 24px
    display: flex
    align-items: center
    gap: 5px
  }

  .inq-date-corner {
    font-size: 11px
    font-weight: 400
    color: var(--ink-mute)
    opacity: 0.7
  }

  .inq-corner-sep {
    font-size: 9px
    color: var(--ink-mute)
    opacity: 0.3
  }

  .inq-id {
    font-size: 9px
    font-weight: 600
    letter-spacing: 0.6px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.35
  }

  .show-more-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 10px 24px
    flex-shrink: 0
    border-top: 0.5px solid var(--divider)
  }

  .show-more-label {
    font-size: 13px
    font-weight: 600
    color: var(--ink-mute)
  }

  .show-more-label span {
    color: var(--ink-mute)
    opacity: 0.6
    font-size: 11px
    margin-left: 6px
    letter-spacing: 0.3px
  }

  .section-toggle {
    padding: 6px 0
    border-radius: 999px
    min-width: 64px
    text-align: center
    background: var(--base-whisper)
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    font-family: var(--font)
    transition: box-shadow 0.2s ease
    -webkit-tap-highlight-color: transparent
  }

  .section-toggle.collapsed {
    box-shadow: inset 1px 1px 3px var(--neu-dark), inset -1px -1px 3px var(--neu-light)
  }

  .section-toggle.expanded {
    box-shadow: 2px 2px 5px var(--neu-dark), -2px -2px 5px var(--neu-light)
  }

══════════════════════════════════════════════════════════════════════
  ── recover link — hidden in active, accent in archived
══════════════════════════════════════════════════════════════════════

  .recover-link {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.4px
    text-transform: uppercase
    color: var(--accent)
    opacity: 1
    background: none
    border: none
    cursor: pointer
    font-family: var(--font)
    padding: 0
    pointer-events: auto
    display: none
  }

  .recover-link.active-link {
    display: inline
  }

══════════════════════════════════════════════════════════════════════
  ── recover selection mode
══════════════════════════════════════════════════════════════════════

  .recover-mode .inq-item {
    cursor: pointer
  }

  .recover-mode .inq-item:hover {
    outline: 1.5px solid var(--accent)
    outline-offset: -2px
    border-radius: 8px
  }

  .recover-mode .inq-item.selected-recover {
    outline: 2px solid var(--accent)
    outline-offset: -2px
    border-radius: 8px
    background: var(--accent-mute)
  }

══════════════════════════════════════════════════════════════════════
  ── recover action bar
══════════════════════════════════════════════════════════════════════

  .recover-bar {
    display: none
    flex-shrink: 0
    padding: 24px 24px 32px
    gap: 10px
  }

  .recover-bar.visible {
    display: flex
  }

  .recover-confirm {
    flex: 1
    padding: 18px 20px
    border-radius: 999px
    background: var(--accent)
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: var(--base-mute)
    font-family: var(--font)
    box-shadow: var(--neu-sm)
    transition: transform 0.12s ease
  }

  .light .recover-confirm {
    box-shadow: 2px 2px 6px rgba(146,158,178,0.25), -1px -1px 4px rgba(255,255,255,0.7)
  }

  .recover-confirm:active {
    transform: scale(0.97)
  }

  .recover-cancel {
    padding: 18px 18px
    border-radius: 999px
    background: var(--base-whisper)
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 700
    color: var(--ink-mute)
    font-family: var(--font)
    box-shadow: 3px 3px 7px var(--neu-dark), -3px -3px 7px var(--neu-light)
    transition: transform 0.12s ease, box-shadow 0.12s ease
  }

  .light .recover-cancel {
    box-shadow: 2px 2px 6px rgba(146,158,178,0.25), -2px -2px 5px rgba(255,255,255,0.85)
  }

  .recover-cancel:active {
    transform: scale(0.97)
    box-shadow: var(--neu-in)
  }

  #pane-archived .inq-item {
    opacity: 0.7
    pointer-events: auto
    cursor: pointer
  }

  #pane-archived .inq-title {
    color: var(--ink-mute)
    text-shadow: -1px -1px 1px rgba(6,9,15,0.7), 1px 1px 1px rgba(72,86,110,0.3)
  }

  .light #pane-archived .inq-title {
    color: var(--ink-mute)
    text-shadow: -1px -1px 1px rgba(255,255,255,0.8), 1px 1px 1px rgba(146,158,178,0.4)
  }

  #pane-archived .inq-sender {
    opacity: 0.5
  }

  #pane-archived .inq-date-corner {
    opacity: 0.4
  }

  #pane-archived .inq-id {
    opacity: 0.2
  }

  #pane-archived .inq-corner-sep {
    opacity: 0.2
  }

  #pane-archived .show-more-label {
    opacity: 0.5
  }

  .inq-cta-wrap {
    padding: 24px 24px 32px
    flex-shrink: 0
  }

  .inq-cta-btn {
    width: 100%
    padding: 18px 24px
    border-radius: 999px
    background: var(--sedona)
    border: none
    cursor: pointer
    font-size: 16px
    font-weight: 700
    color: #fff7f3
    font-family: var(--font)
    letter-spacing: -0.2px
    box-shadow: var(--neu-sm)
    transition: transform 0.14s ease, box-shadow 0.14s ease
  }

  .light .inq-cta-btn {
    box-shadow: 2px 2px 6px rgba(146,158,178,0.25), -1px -1px 4px rgba(255,255,255,0.7)
  }

  .inq-cta-btn:active {
    transform: scale(0.97)
    box-shadow: var(--neu-in)
  }

  .cta-btn {
    width: 100%
    padding: 16px 24px
    border-radius: 999px
    background: var(--sedona)
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: #fff7f3
    font-family: var(--font)
    box-shadow: var(--neu-sm)
    transition: transform 0.12s ease, box-shadow 0.12s ease
    margin-top: 8px
  }

  .cta-btn:active {
    transform: scale(0.97)
    box-shadow: var(--neu-in)
  }

══════════════════════════════════════════════════════════════════════
  ── backdrop scrim with blur
══════════════════════════════════════════════════════════════════════

  .drawer-scrim {
    position: absolute
    inset: 0
    z-index: 30
    background: var(--scrim)
    opacity: 0
    pointer-events: none
    backdrop-filter: blur(0px)
    -webkit-backdrop-filter: blur(0px)
    transition: opacity 0.46s ease, backdrop-filter 0.46s ease, -webkit-backdrop-filter 0.46s ease
  }

  .drawer-scrim.open {
    opacity: 1
    pointer-events: auto
  }

  .drawer-scrim.closing {
    opacity: 0
    transition: opacity 0.28s ease, backdrop-filter 0.28s ease, -webkit-backdrop-filter 0.28s ease
  }

══════════════════════════════════════════════════════════════════════
  ── drawer sheet
══════════════════════════════════════════════════════════════════════

  .drawer {
    position: absolute
    top: 12px
    right: 0
    bottom: 12px
    width: 88%
    z-index: 31
    background: var(--base)
    border-radius: 25px 0 0 25px
    display: flex
    flex-direction: column
    overflow: hidden
    will-change: transform
    transform: translateX(100%)
    transition: transform 0.46s cubic-bezier(0.16, 1, 0.3, 1), background 0.3s ease
  }

  .drawer.open {
    transform: translateX(0)
  }

  .drawer.closing {
    transform: translateX(100%)
    transition: transform 0.28s cubic-bezier(0.4, 0, 1, 1)
  }

══════════════════════════════════════════════════════════════════════
  ── drag handle
══════════════════════════════════════════════════════════════════════

  .drw-handle {
    display: flex
    justify-content: center
    padding: 12px 0 0
    flex-shrink: 0
  }

  .drw-handle-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(--ink-mute)
    opacity: 0.3
  }

══════════════════════════════════════════════════════════════════════
  ── nav row: back btn + hint
══════════════════════════════════════════════════════════════════════

  .drw-nav {
    display: flex
    align-items: center
    gap: 12px
    padding: 16px 24px 0
    flex-shrink: 0
  }

  .drw-back {
    width: 32px
    height: 32px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(--base-whisper)
    display: flex
    align-items: center
    justify-content: center
    color: var(--ink-mute)
    font-size: 16px
    flex-shrink: 0
    box-shadow: 2px 2px 5px var(--neu-dark), -2px -2px 5px var(--neu-light)
    -webkit-tap-highlight-color: transparent
  }

  .drw-hint {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.5px
    text-transform: uppercase
    color: var(--ink-mute)
  }

══════════════════════════════════════════════════════════════════════
  ── scrollable body
══════════════════════════════════════════════════════════════════════

  .drw-body {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    padding: 0 24px 0
  }

  .drw-body::-webkit-scrollbar {
    display: none
  }

══════════════════════════════════════════════════════════════════════
  ── gem photo placeholder circle
══════════════════════════════════════════════════════════════════════

  .drw-photo-wrap {
    display: flex
    justify-content: center
    padding: 20px 0 18px
  }

  .drw-photo-circle {
    width: 110px
    height: 110px
    border-radius: 50%
    background: var(--base-whisper)
    box-shadow: inset 3px 3px 7px var(--neu-dark), inset -3px -3px 7px var(--neu-light)
    display: flex
    align-items: center
    justify-content: center
    flex-shrink: 0
  }

  .drw-photo-letter {
    font-size: 38px
    font-weight: 800
    color: var(--ink-mute)
    opacity: 0.2
    text-transform: uppercase
    letter-spacing: -1px
  }

══════════════════════════════════════════════════════════════════════
  ── product identity
══════════════════════════════════════════════════════════════════════

  .drw-product-id {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
    margin-bottom: 4px
  }

  .drw-product-title {
    font-size: 26px
    font-weight: 700
    letter-spacing: -0.5px
    line-height: 1.1
    color: var(--ink)
    margin-bottom: 12px
  }

══════════════════════════════════════════════════════════════════════
  ── status pill
══════════════════════════════════════════════════════════════════════

  .drw-status-row {
    margin-bottom: 16px
  }

  .status-pill {
    display: inline-flex
    align-items: center
    gap: 6px
    padding: 7px 14px
    border-radius: 999px
    font-size: 12px
    font-weight: 700
    letter-spacing: 0.5px
  }

  .status-pill.read {
    background: var(--accent-mute)
    color: var(--accent)
  }

  .status-pill.waiting {
    background: var(--base-whisper)
    color: var(--ink-mute)
  }

  .status-dot {
    width: 7px
    height: 7px
    border-radius: 50%
    background: currentColor
  }

══════════════════════════════════════════════════════════════════════
  ── section divider
══════════════════════════════════════════════════════════════════════

  .drw-divider {
    height: 0.5px
    background: var(--divider)
    margin: 0 0 16px
  }

  .drw-message {
    font-size: 14px
    line-height: 1.65
    margin-bottom: 16px
  }

  .user-message {
    font-style: italic
    color: var(--ink-whisper)
  }

  .admin-message {
    font-style: normal
    color: var(--ink)
    padding-left: 12px
    border-left: 2px solid var(--accent)
  }

══════════════════════════════════════════════════════════════════════
  ── show more conversation toggle row
══════════════════════════════════════════════════════════════════════

  .drw-show-more-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 4px 0 10px
  }

  .drw-show-more-label {
    font-size: 11px
    font-weight: 600
    letter-spacing: 0.5px
    color: var(--ink-mute)
    opacity: 0.6
  }

  .drw-more-overflow {
    display: none
  }

  .drw-more-overflow.open {
    display: block
  }

  .drw-details-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 4px 0 10px
  }

  .drw-details-label {
    font-size: 11px
    font-weight: 700
    letter-spacing: 1.4px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
  }

  .drw-details-body {
    display: none
    padding-bottom: 8px
  }

  .drw-details-body.open {
    display: block
  }

  .drw-detail-item {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 10px 0
    border-bottom: 0.5px solid var(--divider)
  }

  .drw-detail-item:last-child {
    border-bottom: none
  }

  .drw-detail-key {
    font-size: 12px
    font-weight: 600
    letter-spacing: 0.5px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.6
  }

  .drw-detail-val {
    font-size: 14px
    font-weight: 500
    color: var(--ink)
  }

══════════════════════════════════════════════════════════════════════
  ── swipedowntoclose on header
══════════════════════════════════════════════════════════════════════

  header.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); header.addEventListener('touchmove', onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── swipe right to close drawer
══════════════════════════════════════════════════════════════════════

  drw.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); drw.addEventListener('touchmove', onMove, {
    passive: false
  }

  ); drw.addEventListener('touchend', onEnd); scrim.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); scrim.addEventListener('touchmove', onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── Supabase: await markRead(item.dataset.id);
══════════════════════════════════════════════════════════════════════

  openDetailDrawer( {
    id: item.dataset.id, title: item.dataset.title, status: item.dataset.status, activity: item.dataset.activity, gem: item.dataset.gem, service: item.dataset.service, weight: item.dataset.weight, notes: item.dataset.notes, reply: item.dataset.reply
  }

══════════════════════════════════════════════════════════════════════
  ── In Next.js: router.push('/#store') or scrollIntoView on #storesection
══════════════════════════════════════════════════════════════════════

  var storeHint = document.querySelector('.home-hint'); if (storeHint) storeHint.scrollIntoView( {
    behavior: smooth', block: 'center
  }

══════════════════════════════════════════════════════════════════════
  ── Reset
══════════════════════════════════════════════════════════════════════

  - {
    box-sizing: border-box
    margin: 0
    padding: 0
  }

══════════════════════════════════════════════════════════════════════
  ── ccgtokens.css
══════════════════════════════════════════════════════════════════════

  * {
    box-sizing: border-box
    margin: 0
    padding: 0
  }

  :root {
    --base: #1c2230
    --base-whisper: #232a3a
    --base-mute: #131825
    --ink: #e4e8ef
    --ink-whisper: #b5bdcc
    --ink-mute: #7d8597
    --accent: #d4ec5a
    --accent-mute: #2f3a20
    --sedona: #c46850
    --divider: rgba(255,255,255,0.06)
    --neu-dark: rgba(6,9,15,0.78)
    --neu-light: rgba(72,86,110,0.42)
    --scrim: rgba(0,0,0,0.45)
    --bg-outer: #0a0d14
    --online: #3ecf82
    --font: -apple-system, BlinkMacSystemFont, 'SF Pro Display', sans-serif
  }

  .light {
    --base: #e8ecf2
    --base-whisper: #dfe4ec
    --base-mute: #f1f4f9
    --ink: #2b3445
    --ink-whisper: #4a5363
    --ink-mute: #8893a6
    --accent: #1a82c8
    --accent-mute: #cee5f5
    --sedona: #8e7470
    --divider: rgba(0,0,0,0.07)
    --neu-dark: rgba(146,158,178,0.55)
    --neu-light: rgba(255,255,255,0.95)
    --scrim: rgba(43,52,69,0.35)
    --bg-outer: #c8cdd8
  }

══════════════════════════════════════════════════════════════════════
  ── Neu shadow helpers
══════════════════════════════════════════════════════════════════════

  :root {
    neu-xs: 2px 2px 5px var(–neu-dark), -2px -2px 5px var(–neu-light)
    neu-sm: 3px 3px 7px var(–neu-dark), -3px -3px 7px var(–neu-light)
    neu-md: 6px 6px 14px var(–neu-dark), -6px -6px 14px var(–neu-light)
    neu-in: inset 3px 3px 6px var(–neu-dark), inset -3px -3px 6px var(–neu-light)
  }

══════════════════════════════════════════════════════════════════════
  ── Pulse animations
══════════════════════════════════════════════════════════════════════

  0%,100% {
    color: #8e7470
  }

  50% {
    color: #1a82c8
  }

══════════════════════════════════════════════════════════════════════
  ── Body / outer
══════════════════════════════════════════════════════════════════════

  body {
    background: var(–bg-outer)
    display: flex
    align-items: center
    justify-content: center
    min-height: 100vh
    font-family: var(–font)
    padding: 40px 20px
    transition: background 0.3s ease
  }

  #theme-switcher {
    position: fixed
    top: 20px
    right: 20px
    z-index: 9999
    display: flex
    gap: 8px
  }

  .theme-btn {
    padding: 8px 18px
    border-radius: 999px
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 700
    font-family: var(–font)
    letter-spacing: 0.5px
  }

  .theme-btn.dark-btn {
    background: #1c2230
    color: #d4ec5a
  }

  .theme-btn.light-btn {
    background: #e8ecf2
    color: #1a82c8
  }

══════════════════════════════════════════════════════════════════════
  ── Phone frame
══════════════════════════════════════════════════════════════════════

  .phone {
    width: 390px
    height: 844px
    border-radius: 50px
    overflow: hidden
    background: var(–base)
    position: relative
    display: flex
    flex-direction: column
    box-shadow: 0 40px 80px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.06)
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── Status bar
══════════════════════════════════════════════════════════════════════

  .status-bar {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 14px 28px 0
    font-size: 12px
    font-weight: 600
    color: var(–ink-mute)
    flex-shrink: 0
    z-index: 5
    position: relative
  }

  .status-notch {
    width: 120px
    height: 28px
    background: var(–base-mute)
    border-radius: 0 0 18px 18px
    margin-top: -14px
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── Scroll area
══════════════════════════════════════════════════════════════════════

  .scroll-area {
    flex: 1
    position: relative
    min-height: 0
    display: flex
    flex-direction: column
  }

  .scroll-fade {
    position: absolute
    left: 0
    right: 0
    height: 24px
    pointer-events: none
    z-index: 4
  }

  .scroll-fade.top {
    top: 0
    background: linear-gradient(to bottom, var(–base), transparent)
  }

  .scroll-fade.bot {
    bottom: 0
    background: linear-gradient(to top, var(–base), transparent)
  }

  .scroll-content {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    min-height: 0
    padding: 0 24px 40px
    scrollbar-width: none
    -webkit-overflow-scrolling: touch
    overscroll-behavior: contain
  }

  .scroll-content::-webkit-scrollbar {
    display: none
  }

══════════════════════════════════════════════════════════════════════
  ── Dashboard header
══════════════════════════════════════════════════════════════════════

  .dash-header {
    display: flex
    justify-content: space-between
    align-items: flex-start
    padding: 20px 0 8px
  }

  .dash-date {
    font-size: 11px
    font-weight: 600
    letter-spacing: 2px
    text-transform: uppercase
    color: var(–ink-mute)
    margin-bottom: 6px
  }

  .dash-title {
    font-size: 58px
    font-weight: 800
    line-height: 1
    color: var(–ink)
    letter-spacing: -2px
  }

  .dash-profile {
    display: flex
    flex-direction: column
    align-items: flex-end
    gap: 4px
    margin-top: 20px
  }

  .dash-profile-gem, .dash-profile-role {
    font-size: 11px
    color: var(–ink-mute)
    font-weight: 500
    text-align: right
  }

  .dash-profile-edit {
    font-size: 10px
    color: var(–ink-mute)
    cursor: pointer
    font-weight: 600
    opacity: 0.5
    letter-spacing: 0.5px
    text-decoration: underline
    background: none
    border: none
  }

  .dash-scroll-hint {
    font-size: 13px
    color: var(–ink-mute)
    font-weight: 500
    text-align: right
    margin-top: 8px
    opacity: 0.7
  }

══════════════════════════════════════════════════════════════════════
  ── Tile strip
══════════════════════════════════════════════════════════════════════

  .tile-strip {
    overflow-x: auto
    scrollbar-width: none
    margin: 0 -24px
    padding: 16px 24px 20px
    -webkit-overflow-scrolling: touch
    overscroll-behavior: contain
  }

  .tile-strip::-webkit-scrollbar {
    display: none
  }

  .tile-row {
    display: flex
    gap: 12px
  }

  .tile {
    min-width: 240px
    border-radius: 20px
    padding: 20px 20px 18px
    background: var(–base)
    flex-shrink: 0
    position: relative
    cursor: pointer
    user-select: none
    box-shadow: var(–neu-sm)
    transition: box-shadow 0.18s ease, transform 0.18s cubic-bezier(0.34,1.2,0.64,1)
    -webkit-tap-highlight-color: transparent
  }

  .tile:active {
    transform: scale(0.96)
    box-shadow: var(–neu-in)
    transition: box-shadow 0.16s ease, transform 0.16s ease
  }

  .tile.is-new {
    background: var(–accent)
  }

  .tile-dot {
    position: absolute
    top: 14px
    right: 14px
    width: 10px
    height: 10px
    border-radius: 50%
  }

  .tile-label {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.2px
    text-transform: uppercase
    margin-bottom: 8px
    opacity: 0.65
  }

  .tile:not(.is-new) .tile-label {
    color: var(–ink-mute)
  }

  .tile.is-new .tile-label {
    color: var(–accent-mute)
  }

  .light .tile.is-new .tile-label {
    color: rgba(255,255,255,0.85)
  }

  .tile-num {
    font-size: 48px
    font-weight: 800
    line-height: 1
    letter-spacing: -1.5px
  }

  .tile:not(.is-new) .tile-num {
    color: var(–ink)
  }

  .tile.is-new .tile-num {
    color: var(–base-mute)
  }

  .light .tile.is-new .tile-num {
    color: #fff
  }

  .tile-msg {
    font-size: 13px
    margin-top: 6px
    font-weight: 500
    opacity: 0.85
  }

  .tile:not(.is-new) .tile-msg {
    color: var(–ink-mute)
  }

  .tile.is-new .tile-msg {
    color: var(–accent-mute)
  }

  .light .tile.is-new .tile-msg {
    color: rgba(255,255,255,0.85)
  }

══════════════════════════════════════════════════════════════════════
  ── Dot colors
══════════════════════════════════════════════════════════════════════

  .tile:not(.is-new) .tile-dot {
    background: var(–accent)
    box-shadow: 0 0 0 2px var(–base)
  }

  .tile.is-new .tile-dot {
    background: #fff
    box-shadow: 0 0 0 2px rgba(255,255,255,0.3)
  }

══════════════════════════════════════════════════════════════════════
  ── Section headers
══════════════════════════════════════════════════════════════════════

  .section-header {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 10px 0
  }

  .section-title {
    font-size: 20px
    font-weight: 800
    transition: color 0.2s ease
  }

  .section-title.idle {
    color: var(–ink-mute)
  }

  .section-title.active {
    color: var(–sedona)
  }

  .section-toggle {
    padding: 6px 0
    border-radius: 999px
    min-width: 64px
    text-align: center
    background: var(–base-whisper)
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 600
    color: var(–ink-mute)
    font-family: var(–font)
    transition: box-shadow 0.2s ease
    -webkit-tap-highlight-color: transparent
  }

  .section-toggle.collapsed {
    box-shadow: inset 1px 1px 3px var(–neu-dark), inset -1px -1px 3px var(–neu-light)
  }

  .section-toggle.expanded {
    box-shadow: 2px 2px 5px var(–neu-dark), -2px -2px 5px var(–neu-light)
  }

  .section-body {
    display: none
  }

  .section-body.open {
    display: block
  }

══════════════════════════════════════════════════════════════════════
  ── Horizontal card scroll
══════════════════════════════════════════════════════════════════════

  .card-scroll {
    overflow-x: auto
    scrollbar-width: none
    margin: 0 -24px
    padding: 8px 24px 20px
    -webkit-overflow-scrolling: touch
    overscroll-behavior: contain
  }

  .card-scroll::-webkit-scrollbar {
    display: none
  }

  .card-row {
    display: flex
    gap: 12px
  }

══════════════════════════════════════════════════════════════════════
  ── Shop card
══════════════════════════════════════════════════════════════════════

  .shop-card {
    border-radius: 12px
    overflow: hidden
    background: transparent
    min-width: 160px
    width: 160px
    flex-shrink: 0
    position: relative
  }

  .shop-card-img {
    height: 180px
    background: var(–base-whisper)
    border-radius: 12px 12px 0 0
    display: flex
    align-items: center
    justify-content: center
    color: var(–ink-mute)
    font-size: 22px
    font-weight: 700
    position: relative
    transition: background 0.3s ease
  }

  .shop-card-img-letter {
    opacity: 0.3
  }

  .shop-card-heart {
    position: absolute
    bottom: -18px
    right: 10px
    width: 36px
    height: 36px
    border-radius: 50%
    background: var(–base)
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    font-size: 16px
    z-index: 1
    transition: transform 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .shop-card-heart:active {
    transform: scale(0.9)
  }

  .shop-card-heart.fav {
    color: #c46850
    box-shadow: inset 2px 2px 4px var(–neu-dark), inset -2px -2px 4px var(–neu-light)
  }

  .shop-card-heart:not(.fav) {
    color: var(–ink-whisper)
    box-shadow: var(–neu-xs)
  }

  .shop-card-footer {
    padding: 10px 12px
    border-radius: 0 0 12px 12px
    font-size: 13px
    font-weight: 600
    color: var(–ink)
    text-align: center
    white-space: nowrap
    overflow: hidden
    text-overflow: ellipsis
    letter-spacing: -0.1px
    background: #2a3142
    transition: background 0.3s ease
  }

  .light .shop-card-footer {
    background: #bcbcc4
  }

══════════════════════════════════════════════════════════════════════
  ── Wide shop card
══════════════════════════════════════════════════════════════════════

  .shop-card-wide {
    border-radius: 16px
    overflow: hidden
    background: transparent
    flex-shrink: 0
    position: relative
    width: 318px
  }

  .shop-card-wide .shop-card-img {
    height: 287px
    border-radius: 16px 16px 0 0
    font-size: 48px
  }

  .shop-card-wide .shop-card-heart {
    bottom: -18px
    right: 16px
  }

  .shop-card-wide-footer {
    height: 48px
    padding: 0 16px
    background: #2a3142
    display: flex
    align-items: center
    font-size: 15px
    font-weight: 600
    color: var(–ink)
    letter-spacing: -0.1px
    transition: background 0.3s ease
  }

  .light .shop-card-wide-footer {
    background: #bcbcc4
  }

══════════════════════════════════════════════════════════════════════
  ── Store layout toggle
══════════════════════════════════════════════════════════════════════

  .store-controls {
    display: flex
    gap: 8px
    align-items: center
  }

  .store-layout-toggle {
    display: flex
    align-items: center
    width: 44px
    height: 24px
    border-radius: 999px
    background: transparent
    border: none
    cursor: pointer
    padding: 0 3px
    transition: box-shadow 0.25s ease
  }

  .store-layout-toggle.wide-on {
    box-shadow: inset 1px 1px 3px var(–neu-dark), inset -1px -1px 3px var(–neu-light)
  }

  .store-layout-knob {
    width: 16px
    height: 16px
    border-radius: 50%
    transition: transform 0.2s cubic-bezier(0.34,1.2,0.64,1), background 0.2s ease, box-shadow 0.2s ease
  }

  .store-layout-knob.off {
    background: transparent
    box-shadow: 2px 2px 4px var(–neu-dark), -2px -2px 4px var(–neu-light)
    transform: translateX(0)
  }

  .store-layout-knob.on {
    background: var(–accent)
    box-shadow: var(–neu-sm)
    transform: translateX(18px)
  }

══════════════════════════════════════════════════════════════════════
  ── Store grid
══════════════════════════════════════════════════════════════════════

  .store-grid-wrap {
    max-height: 460px
    overflow-y: scroll
    overflow-x: hidden
    scrollbar-width: none
    position: relative
    margin-bottom: 24px
    -webkit-overflow-scrolling: touch
    overscroll-behavior: none
  }

  .store-grid-wrap::-webkit-scrollbar {
    display: none
  }

  .store-grid {
    display: grid
    grid-template-columns: 160px 160px
    gap: 12px
    justify-content: center
    padding: 4px 0 12px
  }

  .store-grid.wide {
    display: flex
    flex-direction: column
    align-items: center
    gap: 12px
  }

══════════════════════════════════════════════════════════════════════
  ── Portfolio circles
══════════════════════════════════════════════════════════════════════

  .portfolio-wrap {
    overflow-x: auto
    scrollbar-width: none
    margin: 0 -24px
    padding: 8px 24px 20px
    -webkit-overflow-scrolling: touch
  }

  .portfolio-wrap::-webkit-scrollbar {
    display: none
  }

  .portfolio-row {
    display: flex
    gap: 16px
  }

  .portfolio-circle {
    width: 110px
    height: 110px
    border-radius: 50%
    background: var(–base)
    border: none
    cursor: pointer
    flex-shrink: 0
    display: flex
    align-items: center
    justify-content: center
    color: var(–ink-mute)
    font-size: 28px
    box-shadow: inset 2px 2px 4px var(–neu-dark), inset -2px -2px 4px var(–neu-light)
    transition: all 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .portfolio-circle:active {
    transform: scale(0.96)
    box-shadow: 3px 3px 6px var(–neu-dark), -3px -3px 6px var(–neu-light)
  }

══════════════════════════════════════════════════════════════════════
  ── Review section
══════════════════════════════════════════════════════════════════════

  .review-section {
    padding: 10px 0 40px
  }

  .review-title {
    font-size: 16px
    font-weight: 700
    color: var(–ink)
    margin-bottom: 12px
  }

  .star-row {
    display: flex
    gap: 8px
    margin-bottom: 16px
  }

  .star-btn {
    width: 40px
    height: 40px
    border-radius: 50%
    background: var(–base)
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    font-size: 16px
    color: var(–ink-mute)
    transition: all 0.15s cubic-bezier(0.175, 0.885, 0.32, 1.275)
    box-shadow: var(–neu-xs)
    -webkit-tap-highlight-color: transparent
  }

  .star-btn.active {
    color: #ffb84d
    box-shadow: var(–neu-in)
  }

  .star-btn:active {
    transform: scale(0.88)
  }

  .review-saved {
    margin-bottom: 12px
    font-size: 14px
    font-style: italic
    color: var(–ink-whisper)
    line-height: 1.6
    letter-spacing: 0.1px
    display: none
  }

  .review-saved.visible {
    display: block
  }

  .review-form {
    display: none
    flex-direction: column
    gap: 12px
  }

  .review-form.open {
    display: flex
  }

  .review-textarea {
    width: 100%
    min-height: 88px
    border-radius: 16px
    background: var(–base)
    border: none
    outline: none
    box-shadow: var(–neu-in)
    padding: 12px 14px
    font-size: 14px
    color: var(–ink)
    font-family: var(–font)
    resize: none
  }

  .review-btn-row {
    display: flex
    gap: 10px
  }

  .review-btn-cancel {
    flex: 1
    padding: 12px
    border-radius: 999px
    background: var(–base)
    border: none
    cursor: pointer
    font-size: 11px
    font-weight: 700
    letter-spacing: 1px
    text-transform: uppercase
    color: var(–ink-mute)
    box-shadow: var(–neu-xs)
    font-family: var(–font)
  }

  .review-btn-submit {
    flex: 3
    padding: 12px 20px
    border-radius: 999px
    background: var(–sedona)
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: #fff7f3
    font-family: var(–font)
    box-shadow: var(–neu-sm)
  }

  .review-btn-leave {
    width: 100%
    padding: 15px 24px
    border-radius: 999px
    background: var(–sedona)
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: #fff7f3
    font-family: var(–font)
    box-shadow: var(–neu-sm)
    transition: transform 0.12s ease
  }

  .review-btn-leave:active {
    transform: scale(0.97)
  }

══════════════════════════════════════════════════════════════════════
  ── Panel scrim
══════════════════════════════════════════════════════════════════════

  .panel-scrim {
    position: absolute
    top: 0
    left: 0
    right: 0
    bottom: 100px
    z-index: 15
    background: var(–scrim)
    opacity: 0
    pointer-events: none
    transition: opacity 0.38s ease
  }

  .panel-scrim.open {
    opacity: 1
    pointer-events: auto
  }

══════════════════════════════════════════════════════════════════════
  ── Panel
══════════════════════════════════════════════════════════════════════

  .panel {
    position: absolute
    left: 0
    right: 0
    bottom: 100px
    height: calc(100% - 48px - 100px)
    background: var(–base)
    border-radius: 28px 28px 0 0
    z-index: 18
    display: flex
    flex-direction: column
    transform: translateY(100%)
    transition: transform 0.44s cubic-bezier(0.16, 1, 0.3, 1)
    will-change: transform
    overflow: hidden
  }

  .panel.open {
    transform: translateY(0)
  }

  .panel.closing {
    transform: translateY(100%)
    transition: transform 0.30s cubic-bezier(0.4, 0, 1, 1)
  }

══════════════════════════════════════════════════════════════════════
  ── Panel header
══════════════════════════════════════════════════════════════════════

  .panel-header {
    background: transparent
    border-radius: 28px 28px 0 0
    padding: 10px 20px 16px
    flex-shrink: 0
    position: relative
    cursor: grab
    touch-action: none
    user-select: none
  }

  .panel-drag-pill {
    display: flex
    justify-content: center
    padding-bottom: 12px
  }

  .panel-drag-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(–accent)
    opacity: 0.4
  }

  .panel-breadcrumb {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.8px
    text-transform: uppercase
    color: var(–ink-mute)
    margin-bottom: 5px
  }

  .panel-title {
    font-size: 32px
    font-weight: 800
    letter-spacing: -1px
    line-height: 1.1
    padding-right: 50px
    color: #2a3347
    text-shadow: -1px -1px 1px rgba(6,9,15,0.9), 1px 1px 2px rgba(72,86,110,0.5)
    transition: color 0.2s ease, text-shadow 0.2s ease
  }

  .light .panel-title {
    color: #c8cdd8
    text-shadow: -1px -1px 1px rgba(146,158,178,0.6), 1px 1px 2px rgba(255,255,255,0.95)
  }

  .panel-close-track {
    position: absolute
    top: 11px
    right: 16px
    width: 36px
    height: 36px
    border-radius: 50%
    background: var(–base)
    box-shadow: inset 2px 2px 5px var(–neu-dark), inset -2px -2px 5px var(–neu-light)
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
    transition: background 0.3s ease
  }

  .panel-close {
    width: 26px
    height: 26px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(–base-whisper)
    color: var(–ink-whisper)
    opacity: 0.9
    font-size: 13px
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 3px 3px 7px var(–neu-dark), -2px -2px 5px var(–neu-light)
    transition: all 0.14s ease
  }

  .panel-close:hover {
    opacity: 1
    color: var(–accent)
  }

  .panel-close:active {
    opacity: 1
    color: var(–accent)
    box-shadow: var(–neu-in)
    transform: scale(0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── Inquiries tabs
══════════════════════════════════════════════════════════════════════

  .inquiries-tabs {
    flex-shrink: 0
    margin: 6px 20px 10px
    padding: 4px
    border-radius: 28px
    background: var(–base-mute)
    box-shadow: inset 3px 3px 6px var(–neu-dark), inset -3px -3px 6px var(–neu-light)
    display: flex
    gap: 2px
    transition: background 0.3s ease
  }

  .tab-btn {
    flex: 1
    height: 42px
    border: none
    border-radius: 24px
    cursor: pointer
    font-family: var(–font)
    font-size: 14px
    font-weight: 600
    color: var(–ink-mute)
    background: transparent
    transition: all 0.22s cubic-bezier(0.16, 1, 0.3, 1)
    -webkit-tap-highlight-color: transparent
  }

  .tab-btn.active {
    background: var(–accent)
    color: var(–base-mute)
    box-shadow: 2px 2px 6px var(–neu-dark), -1px -1px 4px var(–neu-light)
  }

══════════════════════════════════════════════════════════════════════
  ── Inquiries list
══════════════════════════════════════════════════════════════════════

  .inquiries-list {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    padding-bottom: 20px
    -webkit-overflow-scrolling: touch
  }

  .inquiries-list::-webkit-scrollbar {
    display: none
  }

  .tab-pane {
    display: none
  }

  .tab-pane.active {
    display: block
  }

  .inquiry-item {
    padding: 19px 24px
    border-bottom: 0.5px solid var(–divider)
    display: flex
    align-items: center
    cursor: pointer
    transition: background 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .inquiry-item:last-child {
    border-bottom: none
  }

  .inquiry-item:active {
    background: var(–base-whisper)
  }

  .inquiry-body {
    flex: 1
    min-width: 0
  }

  .inquiry-id {
    font-size: 10px
    font-weight: 600
    letter-spacing: 0.8px
    text-transform: uppercase
    color: var(–ink-mute)
    opacity: 0.65
    margin-bottom: 3px
  }

  .inquiry-title {
    font-size: 21px
    font-weight: 500
    color: var(–ink)
    line-height: 1.2
    margin-bottom: 4px
    white-space: nowrap
    overflow: hidden
    text-overflow: ellipsis
  }

  .inquiry-status {
    font-size: 13px
    color: var(–ink-mute)
    font-weight: 400
  }

  .inquiry-arrow {
    font-size: 16px
    margin-left: 16px
    flex-shrink: 0
    transition: color 0.2s ease
  }

  .inquiry-arrow.has-activity {
    color: var(–sedona)
  }

  .inquiry-arrow.neutral {
    color: var(–ink-mute)
    opacity: 0.4
  }

══════════════════════════════════════════════════════════════════════
  ── z30 — covers bottom nav (z20), highest except drawer itself
══════════════════════════════════════════════════════════════════════

  .drawer-scrim {
    position: absolute
    inset: 0
    z-index: 30
    background: var(–scrim)
    opacity: 0
    pointer-events: none
    transition: opacity 0.46s ease, backdrop-filter 0.46s ease, -webkit-backdrop-filter 0.46s ease
    backdrop-filter: blur(0px)
    -webkit-backdrop-filter: blur(0px)
  }

  .drawer-scrim.open {
    opacity: 1
    pointer-events: auto
  }

  .drawer-scrim.closing {
    opacity: 0
    transition: opacity 0.28s ease, backdrop-filter 0.28s ease, -webkit-backdrop-filter 0.28s ease
  }

══════════════════════════════════════════════════════════════════════
  ── z31 — absolute top of stack
══════════════════════════════════════════════════════════════════════

  .drawer {
    position: absolute
    top: 12px
    right: 0
    bottom: 12px
    width: 85%
    z-index: 31
    background: var(–base)
    border-radius: 25px 0 0 25px
    display: flex
    flex-direction: column
    overflow: hidden
    will-change: transform
    transform: translateX(100%)
    transition: transform 0.46s cubic-bezier(0.16, 1, 0.3, 1), background 0.3s ease
    overscroll-behavior: contain
  }

  .drawer.open {
    transform: translateX(0)
  }

  .drawer.closing {
    transform: translateX(100%)
    transition: transform 0.28s cubic-bezier(0.4, 0, 1, 1)
  }

  .drawer-handle {
    display: flex
    justify-content: center
    padding: 12px 0 0
    flex-shrink: 0
  }

  .drawer-handle-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(–ink-mute)
    opacity: 0.3
  }

  .drawer-nav {
    display: flex
    align-items: center
    gap: 12px
    padding: 16px 28px 0
    flex-shrink: 0
  }

  .drawer-back {
    width: 32px
    height: 32px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(–base-whisper)
    display: flex
    align-items: center
    justify-content: center
    color: var(–ink-mute)
    font-size: 16px
    flex-shrink: 0
    box-shadow: 2px 2px 5px var(–neu-dark), -2px -2px 5px var(–neu-light)
    -webkit-tap-highlight-color: transparent
  }

  .drawer-hint {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.5px
    text-transform: uppercase
    color: var(–ink-mute)
  }

  .drawer-title {
    padding: 10px 28px 8px
    font-size: 52px
    font-weight: 800
    letter-spacing: -2px
    line-height: 1
    color: var(–ink)
    flex-shrink: 0
  }

  .drawer-items {
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    flex: 1
    padding-bottom: 32px
  }

  .drawer-items::-webkit-scrollbar {
    display: none
  }

  .menu-item {
    display: flex
    align-items: center
    justify-content: space-between
    padding: 22px 28px
    border-bottom: 0.5px dashed var(–divider)
    cursor: pointer
    -webkit-tap-highlight-color: transparent
  }

  .menu-item:active {
    background: var(–base-whisper)
  }

  .menu-label {
    font-size: 26px
    font-weight: 400
    color: var(–ink)
    letter-spacing: -0.5px
    line-height: 1.15
  }

  .menu-sub {
    font-size: 12px
    color: var(–ink-mute)
    margin-top: 2px
    font-weight: 500
  }

  .menu-label-wrap {
    display: flex
    flex-direction: column
  }

  .menu-arrow {
    font-size: 20px
    font-weight: 700
    flex-shrink: 0
    margin-left: 16px
    color: var(–ink-mute)
  }

  .menu-item.is-new .menu-label {
    color: var(–accent)
  }

  .menu-item.is-new .menu-arrow {
    color: var(–sedona)
  }

  .phone:not(.light) .menu-item.is-new .menu-arrow {
    animation: ccg-pulse-dark  2.2s ease-in-out infinite
  }

  .phone.light .menu-item.is-new .menu-arrow {
    animation: ccg-pulse-light 2.2s ease-in-out infinite
  }

══════════════════════════════════════════════════════════════════════
  ── Bottom nav
══════════════════════════════════════════════════════════════════════

  .bottom-nav {
    height: 100px
    background: var(–base)
    box-shadow: 0 -8px 24px var(–neu-dark)
    display: flex
    align-items: center
    justify-content: space-around
    padding: 12px 20px 24px
    flex-shrink: 0
    z-index: 20
    position: relative
    transition: background 0.3s ease
  }

  .nav-btn {
    width: 40px
    height: 40px
    border-radius: 50%
    border: none
    cursor: pointer
    background: transparent
    display: flex
    align-items: center
    justify-content: center
    font-size: 17px
    color: var(–ink-mute)
    transition: all 0.18s cubic-bezier(0.175, 0.885, 0.32, 1.275)
    -webkit-tap-highlight-color: transparent
  }

  .nav-btn:active {
    transform: scale(0.88)
  }

  .nav-btn.fab {
    width: 54px
    height: 54px
    background: var(–base)
    font-size: 20px
    box-shadow: var(–neu-sm)
    transition: background 0.3s ease, box-shadow 0.18s cubic-bezier(0.175, 0.885, 0.32, 1.275), transform 0.18s cubic-bezier(0.175, 0.885, 0.32, 1.275)
  }

  .nav-btn.fab.active {
    background: var(–accent)
  }

  .phone.light .nav-btn.fab.active {
    background: var(–accent)
  }

  .nav-btn.active:not(.fab) {
    color: var(–accent)
  }

══════════════════════════════════════════════════════════════════════
  ── Profile overlay
══════════════════════════════════════════════════════════════════════

  .profile-overlay {
    position: absolute
    inset: 0
    z-index: 35
    background: var(–scrim)
    backdrop-filter: blur(2px)
    -webkit-backdrop-filter: blur(2px)
    display: none
    align-items: center
    justify-content: center
    padding: 0 28px
  }

  .profile-overlay.open {
    display: flex
  }

  .profile-card {
    width: 100%
    background: var(–base)
    padding: 24px 20px
    border-radius: 25px
    box-shadow: 0 8px 32px rgba(0,0,0,0.5)
    transition: background 0.3s ease
  }

  .profile-card-header {
    display: flex
    justify-content: space-between
    align-items: center
    margin-bottom: 20px
  }

  .profile-card-title {
    font-size: 18px
    font-weight: 700
    color: var(–ink)
    letter-spacing: -0.3px
  }

  .profile-card-close {
    width: 28px
    height: 28px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(–base-whisper)
    display: flex
    align-items: center
    justify-content: center
    color: var(–ink-mute)
    font-size: 14px
    box-shadow: 2px 2px 5px var(–neu-dark), -2px -2px 5px var(–neu-light)
  }

  .profile-label {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.2px
    text-transform: uppercase
    color: var(–ink-mute)
    margin-bottom: 6px
  }

  .profile-input {
    width: 100%
    padding: 10px 14px
    border-radius: 12px
    background: var(–base)
    border: none
    outline: none
    box-shadow: inset 2px 2px 4px var(–neu-dark), inset -2px -2px 4px var(–neu-light)
    font-size: 14px
    color: var(–ink)
    font-family: var(–font)
    margin-bottom: 16px
  }

  .role-option {
    padding: 10px 14px
    border-radius: 12px
    cursor: pointer
    font-size: 14px
    font-weight: 400
    color: var(–ink-mute)
    transition: all 0.1s ease
  }

  .role-option.selected {
    font-weight: 600
    color: var(–accent)
    background: var(–base-whisper)
  }

  .profile-save {
    width: 100%
    padding: 14px 20px
    border-radius: 999px
    background: transparent
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: var(–ink-mute)
    font-family: var(–font)
  }

══════════════════════════════════════════════════════════════════════
  ── Portfolio modal
══════════════════════════════════════════════════════════════════════

  function cyclePortfolioZoom() {
    1: portfolioZoom+1
  }

══════════════════════════════════════════════════════════════════════
  ── Online dot / chat breadcrumb
══════════════════════════════════════════════════════════════════════

  #chat-breadcrumb {
    display: flex
    align-items: center
    gap: 5px
  }

══════════════════════════════════════════════════════════════════════
  ── paneltitle.menuactive — accent when bubble menu open
══════════════════════════════════════════════════════════════════════

  .panel-title.menu-active {
    color: var(–accent) !important
    text-shadow: none !important
  }

══════════════════════════════════════════════════════════════════════
  ── Header plus button
══════════════════════════════════════════════════════════════════════

  .header-plus-track {
    position: absolute
    top: 11px
    right: 89px
    width: 36px
    height: 36px
    border-radius: 50%
    background: transparent
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
  }

  .header-plus {
    width: 26px
    height: 26px
    border-radius: 50%
    background: var(–sedona)
    opacity: 0.65
    border: none
    cursor: pointer
    font-size: 17px
    font-weight: 400
    line-height: 1
    color: #fff7f3
    display: flex
    align-items: center
    justify-content: center
    transition: all 0.15s cubic-bezier(0.175, 0.885, 0.32, 1.275)
    flex-shrink: 0
    -webkit-tap-highlight-color: transparent
  }

  .header-plus:hover {
    opacity: 1
  }

  .header-plus:active {
    opacity: 1
    box-shadow: inset 3px 3px 7px rgba(0,0,0,0.45), inset -1px -1px 3px rgba(255,255,255,0.07)
    transform: scale(0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── Sits outside .panel so overflow:hidden never clips it
══════════════════════════════════════════════════════════════════════

  .bubble-menu {
    position: absolute
    top: 108px
    right: 58px
    z-index: 80
    display: flex
    flex-direction: row
    align-items: center
    gap: 10px
    pointer-events: none
  }

  .bubble-menu-btn {
    width: 44px
    height: 44px
    border-radius: 50%
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 4px 4px 10px var(–neu-dark), -2px -2px 6px var(–neu-light)
    opacity: 0
    transform: translateX(20px) scale(0.6)
    transition: opacity 0.2s ease
    -webkit-tap-highlight-color: transparent
  }

  .bubble-menu-btn.save-btn {
    background: var(–base-whisper)
    transition-delay: 0.06s
  }

  .bubble-menu-btn.img-btn {
    background: var(–sedona)
  }

══════════════════════════════════════════════════════════════════════
  ── Chat messages area
══════════════════════════════════════════════════════════════════════

  .chat-messages {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scroll-behavior: smooth
    scrollbar-width: none
    padding: 16px 20px 8px
    display: flex
    flex-direction: column
    gap: 3px
    -webkit-overflow-scrolling: touch
  }

  .chat-messages::-webkit-scrollbar {
    display: none
  }

  .chat-date-stamp {
    text-align: center
    font-size: 10px
    font-weight: 600
    letter-spacing: 1px
    text-transform: uppercase
    color: var(–ink-mute)
    opacity: 0.55
    padding: 12px 0 8px
  }

══════════════════════════════════════════════════════════════════════
  ── Chat bubbles
══════════════════════════════════════════════════════════════════════

  .bubble-row {
    display: flex
    align-items: flex-end
    margin-bottom: 1px
  }

  .bubble-row.user {
    justify-content: flex-end
  }

  .bubble-row.admin {
    justify-content: flex-start
  }

  .bubble {
    max-width: 74%
    padding: 11px 15px
    font-size: 15px
    line-height: 1.45
    font-weight: 400
  }

  .bubble.admin {
    background: var(–base-whisper)
    color: var(–ink)
    border-radius: 4px 18px 18px 18px
    box-shadow: 3px 3px 7px var(–neu-dark), -2px -2px 5px var(–neu-light)
  }

  .bubble.user {
    background: var(–accent)
    color: var(–base-mute)
    border-radius: 18px 4px 18px 18px
    box-shadow: 3px 3px 7px var(–neu-dark), -1px -1px 4px var(–neu-light)
    font-weight: 500
  }

  .bubble-meta {
    font-size: 10px
    font-weight: 500
    color: var(–ink-mute)
    opacity: 0.6
    padding: 1px 4px 6px
    text-align: right
    letter-spacing: 0.3px
  }

  .bubble-meta.admin {
    text-align: left
  }

══════════════════════════════════════════════════════════════════════
  ── Chat input bar
══════════════════════════════════════════════════════════════════════

  .chat-input-bar {
    flex-shrink: 0
    padding: 10px 16px 14px
    display: flex
    align-items: center
    gap: 10px
    border-top: 0.5px solid var(–divider)
    background: var(–base)
    transition: background 0.3s ease
  }

  .chat-input {
    flex: 1
    padding: 12px 18px
    border-radius: 25px
    background: rgba(255,255,255,0.09)
    border: none
    outline: none
    box-shadow: inset 3px 3px 7px var(–neu-dark), inset -2px -2px 5px var(–neu-light)
    font-size: 14px
    color: var(–ink)
    font-family: var(–font)
    resize: none
    line-height: 1.4
    max-height: 80px
    overflow-y: auto
    scrollbar-width: none
    transition: background 0.3s ease, box-shadow 0.2s ease
  }

  .light .chat-input {
    background: rgba(255,255,255,0.9)
  }

  .chat-input::placeholder {
    color: var(–ink-mute)
    opacity: 0.7
  }

  .chat-input:focus {
    box-shadow: inset 4px 4px 9px var(–neu-dark), inset -3px -3px 7px var(–neu-light)
  }

  .chat-input::-webkit-scrollbar {
    display: none
  }

  .send-btn {
    width: 42px
    height: 42px
    border-radius: 50%
    background: var(–sedona)
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    font-size: 18px
    color: #fff7f3
    flex-shrink: 0
    box-shadow: var(–neu-sm)
    transition: transform 0.14s ease, box-shadow 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .send-btn:active {
    transform: scale(0.88)
    box-shadow: inset 2px 2px 5px rgba(0,0,0,0.25)
  }

══════════════════════════════════════════════════════════════════════
  ── Typing indicator
══════════════════════════════════════════════════════════════════════

  0%,60%,100% {
    transform: translateY(0)
    opacity: 0.4
  }

  30% {
    transform: translateY(-4px)
    opacity: 1
  }

  .dot {
    display: inline-block
    width: 6px
    height: 6px
    border-radius: 50%
    background: var(–ink-mute)
    opacity: 0.5
    animation: typing-dot 1.2s ease-in-out infinite
  }

  .dot:nth-child(2) {
    animation-delay: 0.18s
  }

  .dot:nth-child(3) {
    animation-delay: 0.36s
  }

══════════════════════════════════════════════════════════════════════
  ── Data
══════════════════════════════════════════════════════════════════════

  var GEMS = ["citrine","rose quartz","sapphire","emerald","garnet","topaz"]; var ROLES = ["collector","cutter","dealer","student"]; var TILES = [ {
    label: PRODUCT INQUIRY", num:"1", msg:"admin read",     isNew:false, action:"inquiries
  }

  , {
    label: education",         sub:"12 articles",         hasNew:false
  }

  , ]; var MENU_ITEMS = [ {
    label: cut wizard",        sub:"start a new report",  hasNew:false
  }

══════════════════════════════════════════════════════════════════════
  ── App state
══════════════════════════════════════════════════════════════════════

  var favs = {
    garnet: true
  }

══════════════════════════════════════════════════════════════════════
  ── ccgpanel.js
══════════════════════════════════════════════════════════════════════

  header.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); header.addEventListener('touchmove', onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── Swiperighttoclose on drawer
══════════════════════════════════════════════════════════════════════

  drw.addEventListener("touchstart",onStart, {
    passive: true
  }

  ); drw.addEventListener("touchmove", onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── Chat helpers
══════════════════════════════════════════════════════════════════════

  return new Date().toLocaleTimeString([], {
    hour: 2-digit", minute:"2-digit", hour12:false
  }

  function showTyping(container) {
    display: flex
    gap: 5px
    align-items: center
    padding: 14px 18px
  }

  ); _bubbleMenu = initBubbleMenu( {
    triggerId: header-plus", menuId:"bubble-menu", buttons:["save-btn","upload-btn"], panelId:"chat-panel", titleId:"chat-panel-title
  }

  var msgs=document.getElementById("chat-messages"); var nodes=msgs.querySelectorAll(".bubble,.chat-date-stamp,.bubble-meta"); var today=new Date().toLocaleDateString("en-US", {
    year: numeric",month:"long",day:"numeric
  }

  else if(el.classList.contains("bubble")) {
    you: :"admin: ")+el.textContent.trim())
  }

  ); lines.push("");lines.push("----------------------------------------");lines.push("end of transcript"); var blob=new Blob([lines.join("\n")], {
    type: text/plain
  }

══════════════════════════════════════════════════════════════════════
  ── ccgtheme.css
══════════════════════════════════════════════════════════════════════

  body {
    background: var(--bg-outer)
    display: flex
    align-items: center
    justify-content: center
    min-height: 100vh
    font-family: var(--font)
    padding: 40px 20px
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── devonly — remove in production
══════════════════════════════════════════════════════════════════════

  #theme-switcher {
    position: fixed
    top: 20px
    right: 20px
    z-index: 9999
    display: flex
    gap: 8px
  }

  .theme-btn {
    padding: 8px 18px
    border-radius: 999px
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 700
    font-family: var(--font)
    letter-spacing: 0.5px
  }

  .theme-btn.dark-btn {
    background: #1c2230
    color: #d4ec5a
  }

  .theme-btn.light-btn {
    background: #e8ecf2
    color: #1a82c8
  }

══════════════════════════════════════════════════════════════════════
  ── ccgphoneframe.css
══════════════════════════════════════════════════════════════════════

  .phone {
    width: 390px
    height: 844px
    border-radius: 50px
    overflow: hidden
    background: var(--base)
    position: relative
    display: flex
    flex-direction: column
    box-shadow: 0 40px 80px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.06)
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── ccgstatusbar.css
══════════════════════════════════════════════════════════════════════

  .status-bar {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 14px 28px 0
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    flex-shrink: 0
    position: relative
    z-index: 5
  }

  .status-notch {
    width: 120px
    height: 28px
    background: var(--base-mute)
    border-radius: 0 0 18px 18px
    margin-top: -14px
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── zindex 20 — always above panels and scrim
══════════════════════════════════════════════════════════════════════

  .bottom-nav {
    height: 100px
    background: var(--base)
    box-shadow: 0 -8px 24px var(--neu-dark)
    display: flex
    align-items: center
    justify-content: space-around
    padding: 12px 20px 24px
    flex-shrink: 0
    z-index: 20
    position: relative
    transition: background 0.3s ease
  }

  .nav-btn {
    width: 40px
    height: 40px
    border-radius: 50%
    border: none
    cursor: pointer
    background: transparent
    display: flex
    align-items: center
    justify-content: center
    font-size: 17px
    color: var(--ink-mute)
    transition: all 0.18s cubic-bezier(0.175, 0.885, 0.32, 1.275)
  }

  .nav-btn:active {
    transform: scale(0.88)
  }

  .nav-btn.fab {
    width: 54px
    height: 54px
    background: var(--base)
    font-size: 20px
    box-shadow: 3px 3px 7px var(--neu-dark), -3px -3px 7px var(--neu-light)
  }

  .nav-btn.active:not(.fab) {
    color: var(--accent)
  }

══════════════════════════════════════════════════════════════════════
  ── Scrim: zindex 15, stops at nav (bottom:100px) so nav stays visible
══════════════════════════════════════════════════════════════════════

  .panel-scrim {
    position: absolute
    top: 0
    left: 0
    right: 0
    bottom: 100px
    z-index: 15
    background: var(--scrim)
    opacity: 0
    pointer-events: none
    transition: opacity 0.38s ease
  }

  .panel-scrim.open {
    opacity: 1
    pointer-events: auto
  }

══════════════════════════════════════════════════════════════════════
  ── Panel: zindex 18, bottom:100px, slides up behind nav
══════════════════════════════════════════════════════════════════════

  .panel {
    position: absolute
    left: 0
    right: 0
    bottom: 100px
    height: calc(100% - 48px - 100px)
    background: var(--base)
    border-radius: 28px 28px 0 0
    z-index: 18
    display: flex
    flex-direction: column
    transform: translateY(100%)
    transition: transform 0.44s cubic-bezier(0.16, 1, 0.3, 1)
    will-change: transform
    overflow: hidden
  }

  .panel.open {
    transform: translateY(0)
  }

  .panel.closing {
    transform: translateY(100%)
    transition: transform 0.30s cubic-bezier(0.4, 0, 1, 1)
  }

══════════════════════════════════════════════════════════════════════
  ── Transparent, fullwidth drag zone for swipetoclose
══════════════════════════════════════════════════════════════════════

  .panel-header {
    background: transparent
    border-radius: 28px 28px 0 0
    padding: 10px 20px 14px
    flex-shrink: 0
    position: relative
    cursor: grab
    touch-action: none
    user-select: none
  }

  .panel-drag-pill {
    display: flex
    justify-content: center
    padding-bottom: 10px
  }

  .panel-drag-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(--accent)
    opacity: 0.4
  }

  .panel-breadcrumb {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.8px
    text-transform: uppercase
    margin-bottom: 4px
    display: flex
    align-items: center
    gap: 5px
  }

══════════════════════════════════════════════════════════════════════
  ── Sunken text — slightly lighter/darker than base, pressed into surface
══════════════════════════════════════════════════════════════════════

  .panel-title {
    font-size: 30px
    font-weight: 800
    letter-spacing: -1px
    line-height: 1.1
    transition: color 0.2s ease, text-shadow 0.2s ease
    color: #2a3347
    text-shadow: -1px -1px 1px rgba(6,9,15,0.9), 1px 1px 2px rgba(72,86,110,0.5)
  }

  .light .panel-title {
    color: #c8cdd8
    text-shadow: -1px -1px 1px rgba(146,158,178,0.6), 1px 1px 2px rgba(255,255,255,0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── Active state when bubble menu is open
══════════════════════════════════════════════════════════════════════

  .panel-title.menu-active {
    color: var(--accent) !important
    text-shadow: none !important
  }

══════════════════════════════════════════════════════════════════════
  ── Plus button track — flat, transparent, just a size container
══════════════════════════════════════════════════════════════════════

  .header-plus-track {
    position: absolute
    top: 11px
    right: 89px
    width: 36px
    height: 36px
    border-radius: 50%
    background: transparent
    border: 1.5px solid transparent
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
  }

══════════════════════════════════════════════════════════════════════
  ── Plus button — flat, dimmed sedona at rest, sinks on active
══════════════════════════════════════════════════════════════════════

  .header-plus {
    width: 26px
    height: 26px
    border-radius: 50%
    background: var(--sedona)
    opacity: 0.65
    box-shadow: none
    border: none
    cursor: pointer
    font-size: 17px
    font-weight: 400
    line-height: 1
    color: #fff7f3
    display: flex
    align-items: center
    justify-content: center
    transition: all 0.15s cubic-bezier(0.175, 0.885, 0.32, 1.275)
    flex-shrink: 0
  }

  .header-plus:hover {
    opacity: 1
  }

  .header-plus:active {
    opacity: 1
    background: var(--sedona)
    color: #fff7f3
    box-shadow: inset 3px 3px 7px rgba(0,0,0,0.45), inset -1px -1px 3px rgba(255,255,255,0.07)
    transform: scale(0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── Close button track — sunken groove
══════════════════════════════════════════════════════════════════════

  .panel-close-track {
    position: absolute
    top: 11px
    right: 16px
    width: 36px
    height: 36px
    border-radius: 50%
    background: var(--base)
    box-shadow: inset 2px 2px 5px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
  }

══════════════════════════════════════════════════════════════════════
  ── Close button — raised inside the track
══════════════════════════════════════════════════════════════════════

  .panel-close {
    width: 26px
    height: 26px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(--base-whisper)
    color: var(--ink-whisper)
    opacity: 0.9
    font-size: 13px
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 3px 3px 7px var(--neu-dark), -2px -2px 5px var(--neu-light)
    transition: all 0.14s ease
    z-index: 2
  }

  .panel-close:hover {
    opacity: 1
    color: var(--accent)
  }

  .panel-close:active {
    opacity: 1
    color: var(--accent)
    box-shadow: inset 3px 3px 7px var(--neu-dark), inset -1px -1px 3px var(--neu-light)
    transform: scale(0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── Floating bubble icon menu — horizontal, slides left from +
══════════════════════════════════════════════════════════════════════

  .bubble-menu {
    position: absolute
    top: 108px
    right: 58px
    z-index: 80
    display: flex
    flex-direction: row
    align-items: center
    gap: 10px
    pointer-events: none
  }

  .bubble-menu-btn {
    width: 44px
    height: 44px
    border-radius: 50%
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 4px 4px 10px var(--neu-dark), -2px -2px 6px var(--neu-light)
    opacity: 0
    transform: translateX(20px) scale(0.6)
    transition: opacity 0.2s ease, transform 0.32s cubic-bezier(0.34, 1.56, 0.64, 1)
  }

  .bubble-menu-btn.save-btn {
    background: var(--base-whisper)
    transition-delay: 0.06s
  }

  .bubble-menu-btn.img-btn {
    background: var(--sedona)
  }

══════════════════════════════════════════════════════════════════════
  ── ccgchatmessages.css
══════════════════════════════════════════════════════════════════════

  .chat-messages {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scroll-behavior: smooth
    scrollbar-width: none
    padding: 16px 20px 8px
    display: flex
    flex-direction: column
    gap: 3px
    -webkit-overflow-scrolling: touch
  }

  .chat-messages::-webkit-scrollbar {
    display: none
  }

  .chat-date-stamp {
    text-align: center
    font-size: 10px
    font-weight: 600
    letter-spacing: 1px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.55
    padding: 12px 0 8px
  }

  .bubble-row {
    display: flex
    align-items: flex-end
    margin-bottom: 1px
  }

  .bubble-row.user {
    justify-content: flex-end
  }

  .bubble-row.admin {
    justify-content: flex-start
  }

══════════════════════════════════════════════════════════════════════
  ── ccgchatbubble — user variant
══════════════════════════════════════════════════════════════════════

  .bubble {
    max-width: 74%
    padding: 11px 15px
    font-size: 15px
    line-height: 1.45
    font-weight: 400
  }

  .bubble.admin {
    background: var(--base-whisper)
    color: var(--ink)
    border-radius: 4px 18px 18px 18px
    box-shadow: 3px 3px 7px var(--neu-dark), -2px -2px 5px var(--neu-light)
  }

  .bubble.user {
    background: var(--accent)
    color: var(--base-mute)
    border-radius: 18px 4px 18px 18px
    box-shadow: 3px 3px 7px var(--neu-dark), -1px -1px 4px var(--neu-light)
    font-weight: 500
  }

  .bubble-meta {
    font-size: 10px
    font-weight: 500
    color: var(--ink-mute)
    opacity: 0.6
    padding: 1px 4px 6px
    text-align: right
    letter-spacing: 0.3px
  }

  .bubble-meta.admin {
    text-align: left
  }

══════════════════════════════════════════════════════════════════════
  ── ccgchatinput.css
══════════════════════════════════════════════════════════════════════

  .chat-input-bar {
    flex-shrink: 0
    padding: 10px 16px 14px
    display: flex
    align-items: center
    gap: 10px
    border-top: 0.5px solid var(--divider)
    background: var(--base)
    transition: background 0.3s ease
  }

══════════════════════════════════════════════════════════════════════
  ── Sunken white input — 25px radius
══════════════════════════════════════════════════════════════════════

  .chat-input {
    flex: 1
    padding: 12px 18px
    border-radius: 25px
    background: rgba(255,255,255,0.09)
    border: none
    outline: none
    box-shadow: inset 3px 3px 7px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    font-size: 14px
    color: var(--ink)
    font-family: var(--font)
    resize: none
    line-height: 1.4
    max-height: 80px
    overflow-y: auto
    scrollbar-width: none
    transition: background 0.3s ease, box-shadow 0.2s ease
  }

  .light .chat-input {
    background: rgba(255,255,255,0.9)
  }

  .chat-input::placeholder {
    color: var(--ink-mute)
    opacity: 0.7
  }

  .chat-input:focus {
    box-shadow: inset 4px 4px 9px var(--neu-dark), inset -3px -3px 7px var(--neu-light)
  }

  .chat-input::-webkit-scrollbar {
    display: none
  }

══════════════════════════════════════════════════════════════════════
  ── Send button — sedona always
══════════════════════════════════════════════════════════════════════

  .send-btn {
    width: 42px
    height: 42px
    border-radius: 50%
    background: var(--sedona)
    border: none
    cursor: pointer
    display: flex
    align-items: center
    justify-content: center
    font-size: 18px
    color: #fff7f3
    flex-shrink: 0
    box-shadow: 3px 3px 7px var(--neu-dark), -2px -2px 5px var(--neu-light)
    transition: transform 0.14s ease, box-shadow 0.14s ease
  }

  .send-btn:active {
    transform: scale(0.88)
    box-shadow: inset 2px 2px 5px rgba(0,0,0,0.25)
  }

══════════════════════════════════════════════════════════════════════
  ── ccgtypingindicator.css
══════════════════════════════════════════════════════════════════════

  0%,60%,100% {
    transform: translateY(0)
    opacity: 0.4
  }

  30% {
    transform: translateY(-4px)
    opacity: 1
  }

  .dot {
    display: inline-block
    width: 6px
    height: 6px
    border-radius: 50%
    background: var(--ink-mute)
    opacity: 0.5
    animation: typing-dot 1.2s ease-in-out infinite
  }

  .dot:nth-child(2) {
    animation-delay: 0.18s
  }

  .dot:nth-child(3) {
    animation-delay: 0.36s
  }

══════════════════════════════════════════════════════════════════════
  ── ccgchatbubble.js
══════════════════════════════════════════════════════════════════════

  return new Date().toLocaleTimeString([], {
    hour: 2-digit', minute: '2-digit', hour12: false
  }

══════════════════════════════════════════════════════════════════════
  ── ccgtypingindicator.js
══════════════════════════════════════════════════════════════════════

  function showTyping(container) {
    display: flex
    gap: 5px
    align-items: center
    padding: 14px 18px
  }

══════════════════════════════════════════════════════════════════════
  ── ccgchatpanel.js
══════════════════════════════════════════════════════════════════════

  ); // Bubble menu — + reveals save + upload circles _bubbleMenu = initBubbleMenu( {
    triggerId: header-plus
    menuId: bubble-menu
    buttons: ['save-btn', 'upload-btn']
    panelId: chat-panel
    titleId: panel-title
  }

  // var urlResult = supabase.storage.from('chat-attachments').getPublicUrl(path) // await supabase.from('chat_messages').insert( {
    user_id: USER_ID, content: urlResult.data.publicUrl
    type: image', sender_role: 'user', read: false
  }

  ); // Input bar initChatInput( {
    inputId: chat-input
    sendId: send-btn
    onSend: handleSend
  }

  // var result = await supabase.from('chat_messages') // .select('id,content,type,sender_role,created_at,read') // .eq('user_id', USER_ID) // .order('created_at', {
    ascending: true
  }

  // var type = msg.sender_role === 'admin' ? 'admin' : 'user' // msgs.appendChild(createBubble(msg.content, type)) // msgs.appendChild(createMeta( // new Date(msg.created_at).toLocaleTimeString([], {
    hour: 2-digit',minute:'2-digit',hour12:false
  }

  // supabase.channel('chat:' + USER_ID) // .on('postgres_changes', {
    event: INSERT', schema: 'public', table: 'chat_messages
    filter: user_id=eq.' + USER_ID
  }

  // var msg = payload.new // if (msg.sender_role !== 'admin') return // var msgs = document.getElementById('chat-messages') // msgs.appendChild(createBubble(msg.content, 'admin')) // msgs.appendChild(createMeta( // new Date(msg.created_at).toLocaleTimeString([], {
    hour: 2-digit',minute:'2-digit',hour12:false
  }

  var msgs = document.getElementById('chat-messages'); var t = nowTime(); msgs.appendChild(createBubble(text, 'user')); var meta = createMeta('sent · ' + t, 'user'); msgs.appendChild(meta); scrollToBottom(); // SUPABASE 4: Insert to database // supabase.from('chat_messages').insert( {
    user_id: USER_ID, content: text
    type: text', sender_role: 'user', read: false
  }

  // await supabase.from('chat_messages') // .update( {
    read: true
  }

  // var channel = supabase.channel('presence:admin') // channel.on('presence', {
    event: sync
  }

  var msgs = document.getElementById('chat-messages'); var nodes = msgs.querySelectorAll('.bubble, .chat-date-stamp, .bubble-meta'); var lines = ['CCG CHAT TRANSCRIPT', 'exported ' + new Date().toLocaleDateString('en-US', {
    year: numeric',month:'long',day:'numeric
  }

  else if (el.classList.contains('bubble')) {
    you: : 'admin: ') + el.textContent.trim())
  }

  ); lines.push(''); lines.push('----------------------------------------'); lines.push('end of transcript'); var blob = new Blob([lines.join('\n')], {
    type: text/plain
  }

══════════════════════════════════════════════════════════════════════
  ── Boot
══════════════════════════════════════════════════════════════════════

  ); </script> </body> </html> <!DOCTYPE html> <html> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1"> <title>CCG · Service Requests Panel</title> <!-- ============================================================ CCG SERVICE REQUESTS — COMPONENT REFERENCE FILE ============================================================ COMPONENT TREE ───────────────────────────────────────── <PhoneShell> <StatusBar /> <HomeScreen /> <PanelScrim /> <ServiceRequestsPanel> <PanelHeader /> <TabSwitcher /> active / archived <SRList> <ActivePane> <SectionLabel /> "top three" <SRItem /> ×3 locked <ShowMoreRow /> <SRItem /> ×5 overflow <NewRequestCTA /> "+ new service request" — sedona pill </ActivePane> <ArchivedPane> <RecoverLink /> <SRItem /> ×3 locked <ShowMoreRow /> <SRItem /> ×4 overflow <RecoverBar /> </ArchivedPane> </SRList> </ServiceRequestsPanel> <DrawerScrim /> <SRDetailDrawer> opens when tapping an SR item <DrawerHandle /> <DrawerNav /> <DrawerBody> <SRPhotoCircle /> <SRId /> <SRTitle /> <StatusPill /> <Divider /> <SRDescription /> italic — user's original request <AdminReply /> accent border — if admin replied <ShowMoreConversation /> <Divider /> <SRDetailsToggle /> <SRDetailRows /> gem, service, weight, notes <ContactAdminCTA /> </DrawerBody> </SRDetailDrawer> <NewSRDrawer> opens when tapping "+ new service request" <DrawerHandle /> <DrawerNav /> <DrawerBody> <NewSRForm> <FormTitle /> <FormField /> gem type <FormField /> service type <FormField /> weight / size <FormField /> special notes <SubmitCTA /> </NewSRForm> </DrawerBody> </NewSRDrawer> <BottomNav /> </PhoneShell> ============================================================ --> <style> * {
    box-sizing: border-box
    margin: 0
    padding: 0
  }

══════════════════════════════════════════════════════════════════════
  ── 1. TOKENS
══════════════════════════════════════════════════════════════════════

  :root, .phone {
    --base: #1c2230
    --base-whisper: #232a3a
    --base-mute: #131825
    --ink: #e4e8ef
    --ink-whisper: #b5bdcc
    --ink-mute: #7d8597
    --accent: #d4ec5a
    --accent-mute: #2f3a20
    --sedona: #c3563b
    --divider: rgba(255,255,255,0.06)
    --scrim: rgba(0,0,0,0.45)
    --bg-outer: #0a0d14
    --font: -apple-system, BlinkMacSystemFont, 'SF Pro Display', sans-serif
    --complete-text: #3ecf82
    --complete-bg: rgba(62,207,130,0.15)
    --progress-bg: rgba(212,236,90,0.15)
    --neu-dark: rgba(6,9,15,0.78)
    --neu-light: rgba(72,86,110,0.42)
    --panel-title-color: #3d5070
    --pulse-a: #c3563b
    --pulse-b: #d4ec5a
    --neu-xs: 2px 2px 5px var(--neu-dark),-2px -2px 5px var(--neu-light)
    --neu-sm: 3px 3px 7px var(--neu-dark),-3px -3px 7px var(--neu-light)
    --neu-in: inset 3px 3px 6px var(--neu-dark),inset -3px -3px 6px var(--neu-light)
  }

  .light {
    --base: #e8ecf2
    --base-whisper: #dfe4ec
    --base-mute: #f1f4f9
    --ink: #2b3445
    --ink-whisper: #4a5363
    --ink-mute: #8893a6
    --accent: #1a82c8
    --accent-mute: #cee5f5
    --sedona: #976a63
    --divider: rgba(0,0,0,0.07)
    --scrim: rgba(43,52,69,0.35)
    --bg-outer: #c8cdd8
    --neu-dark: rgba(146,158,178,0.55)
    --neu-light: rgba(255,255,255,0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── 2. PREVIEW CHROME
══════════════════════════════════════════════════════════════════════

  body {
    background: var(--bg-outer)
    display: flex
    align-items: center
    justify-content: center
    min-height: 100vh
    font-family: var(--font)
    padding: 32px 16px
    transition: background 0.3s ease
  }

  .theme-row {
    position: fixed
    top: 16px
    right: 16px
    z-index: 9999
    display: flex
    gap: 8px
  }

  .tbtn {
    padding: 7px 16px
    border-radius: 999px
    border: none
    cursor: pointer
    font-size: 11px
    font-weight: 700
    font-family: var(--font)
    letter-spacing: 0.5px
  }

  .tbtn.dark {
    background: var(--base)
    color: var(--accent)
  }

  .tbtn.lite {
    background: var(--base-mute)
    color: var(--accent)
  }

  .phone {
    width: 390px
    height: 844px
    border-radius: 50px
    overflow: hidden
    background: var(--base)
    position: relative
    display: flex
    flex-direction: column
    box-shadow: 0 40px 80px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.06)
    transition: background 0.3s ease
  }

  .status-bar {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 14px 28px 0
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    flex-shrink: 0
    position: relative
    z-index: 5
  }

  .notch {
    width: 120px
    height: 28px
    background: var(--base-mute)
    border-radius: 0 0 18px 18px
    margin-top: -14px
    transition: background 0.3s ease
  }

  .home {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    padding: 24px 24px 40px
    scrollbar-width: none
  }

  .home::-webkit-scrollbar {
    display: none
  }

  .home-date {
    font-size: 11px
    font-weight: 600
    letter-spacing: 2px
    text-transform: uppercase
    color: var(--ink-mute)
    margin-bottom: 12px
  }

  .home-title {
    font-size: 58px
    font-weight: 800
    line-height: 1
    color: var(--ink)
    letter-spacing: -2px
    margin-bottom: 4px
  }

  .home-hint {
    font-size: 13px
    color: var(--ink-mute)
    margin-top: 24px
    opacity: 0.5
  }

══════════════════════════════════════════════════════════════════════
  ── 3. BOTTOM NAV
══════════════════════════════════════════════════════════════════════

  .bottom-nav {
    height: 100px
    background: var(--base)
    box-shadow: 0 -8px 24px var(--neu-dark)
    display: flex
    align-items: center
    justify-content: space-around
    padding: 12px 20px 24px
    flex-shrink: 0
    z-index: 20
    position: relative
    transition: background 0.3s ease
  }

  .nav-btn {
    width: 40px
    height: 40px
    border-radius: 50%
    border: none
    cursor: pointer
    background: transparent
    display: flex
    align-items: center
    justify-content: center
    font-size: 17px
    color: var(--ink-mute)
    transition: all 0.18s cubic-bezier(0.175,0.885,0.32,1.275)
    -webkit-tap-highlight-color: transparent
  }

  .nav-btn:active {
    transform: scale(0.88)
  }

  .nav-btn.fab {
    width: 54px
    height: 54px
    background: var(--base)
    font-size: 20px
    box-shadow: var(--neu-sm)
    transition: background 0.3s ease, box-shadow 0.18s ease, transform 0.18s ease
  }

  .nav-btn.fab.active {
    background: var(--accent)
  }

  .nav-btn.active:not(.fab) {
    color: var(--accent)
  }

══════════════════════════════════════════════════════════════════════
  ── 4. PANEL SYSTEM
══════════════════════════════════════════════════════════════════════

  .panel-scrim {
    position: absolute
    top: 0
    left: 0
    right: 0
    bottom: 100px
    z-index: 15
    background: var(--scrim)
    opacity: 0
    pointer-events: none
    transition: opacity 0.38s ease
  }

  .panel-scrim.open {
    opacity: 1
    pointer-events: auto
  }

  .panel {
    position: absolute
    left: 0
    right: 0
    bottom: 100px
    height: calc(100% - 48px - 100px)
    background: var(--base)
    border-radius: 28px 28px 0 0
    z-index: 18
    display: flex
    flex-direction: column
    transform: translateY(100%)
    transition: transform 0.44s cubic-bezier(0.16,1,0.3,1), background 0.3s ease
    will-change: transform
    overflow: hidden
  }

  .panel.open {
    transform: translateY(0)
  }

  .panel.closing {
    transform: translateY(100%)
    transition: transform 0.30s cubic-bezier(0.4,0,1,1)
  }

  .panel-header {
    background: transparent
    border-radius: 28px 28px 0 0
    padding: 10px 20px 12px
    flex-shrink: 0
    position: relative
    cursor: grab
    touch-action: none
    user-select: none
  }

  .drag-pill {
    display: flex
    justify-content: center
    padding-bottom: 10px
  }

  .drag-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(--accent)
    opacity: 0.4
  }

  .panel-hint {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.6px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.45
    margin-bottom: 6px
    text-align: center
  }

  .panel-title {
    font-size: 30px
    font-weight: 800
    letter-spacing: 0.5px
    line-height: 1.1
    padding-right: 50px
    padding-left: 8px
    padding-bottom: 8px
    margin-top: 16px
    color: var(--panel-title-color)
    text-shadow: -1px -1px 1px var(--neu-dark), 1px 1px 2px var(--neu-light)
    transition: none
  }

  .light .panel-title {
    color: var(--panel-title-color)
    text-shadow: -1px -1px 1px var(--neu-dark), 1px 1px 2px var(--neu-light)
  }

  .panel-title.is-archived {
    color: var(--sedona)
    text-shadow: none
  }

  .panel-close-track {
    position: absolute
    top: 11px
    right: 16px
    width: 36px
    height: 36px
    border-radius: 50%
    background: var(--base)
    box-shadow: inset 2px 2px 5px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    display: flex
    align-items: center
    justify-content: center
    z-index: 2
    transition: background 0.3s ease
  }

  .panel-close {
    width: 26px
    height: 26px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(--base-whisper)
    color: var(--ink-whisper)
    opacity: 0.9
    font-size: 13px
    display: flex
    align-items: center
    justify-content: center
    box-shadow: 3px 3px 7px var(--neu-dark), -2px -2px 5px var(--neu-light)
    transition: all 0.14s ease
  }

  .panel-close:hover {
    opacity: 1
    color: var(--accent)
  }

  .panel-close:active {
    opacity: 1
    color: var(--accent)
    box-shadow: var(--neu-in)
    transform: scale(0.95)
  }

══════════════════════════════════════════════════════════════════════
  ── 5. TAB SWITCHER
══════════════════════════════════════════════════════════════════════

  .tabs-wrap {
    flex-shrink: 0
    padding: 4px 32px 12px
  }

  .tabs {
    padding: 3px
    border-radius: 20px
    background: var(--base-mute)
    box-shadow: inset 2px 2px 5px var(--neu-dark), inset -2px -2px 5px var(--neu-light)
    display: flex
    position: relative
    overflow: hidden
  }

  .tab-highlight {
    position: absolute
    top: 3px
    bottom: 3px
    width: calc(50% - 3px)
    border-radius: 16px
    background: var(--accent)
    box-shadow: 2px 2px 5px var(--neu-dark), -1px -1px 3px var(--neu-light)
    left: 3px
    pointer-events: none
    transition: none
  }

  .tab-highlight.right {
    transform: translateX(100%)
  }

  .tab-btn {
    flex: 1
    height: 30px
    border: none
    border-radius: 16px
    cursor: pointer
    font-family: var(--font)
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    background: transparent
    position: relative
    z-index: 1
    transition: color 0.18s ease
    -webkit-tap-highlight-color: transparent
  }

  .tab-btn.active {
    color: var(--base-mute)
  }

  .light .tab-btn.active {
    color: var(--base-mute)
  }

══════════════════════════════════════════════════════════════════════
  ── 6. SR LIST
══════════════════════════════════════════════════════════════════════

  .sr-list {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    -webkit-overflow-scrolling: touch
    min-height: 0
  }

  .sr-list::-webkit-scrollbar {
    display: none
  }

  .tab-pane {
    width: 100%
    display: none
    flex-direction: column
    flex-shrink: 0
  }

  .tab-pane.active {
    display: flex
  }

  .pane-locked {
    flex-shrink: 0
  }

  .pane-overflow {
    flex-shrink: 0
    display: none
  }

  .pane-overflow.open {
    display: block
  }

  .sr-section-label {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.8px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
    padding: 12px 24px 6px
    display: flex
    align-items: center
    justify-content: space-between
  }

══════════════════════════════════════════════════════════════════════
  ── 7. SR ITEM
══════════════════════════════════════════════════════════════════════

  .sr-item {
    padding: 16px 24px 26px
    border-bottom: 0.5px solid var(--divider)
    display: flex
    align-items: center
    cursor: pointer
    position: relative
    transition: background 0.14s ease
    -webkit-tap-highlight-color: transparent
  }

  .sr-item:last-child {
    border-bottom: none
  }

  .sr-item:active {
    background: var(--base-whisper)
  }

  .sr-body {
    flex: 1
    min-width: 0
    padding-right: 8px
  }

  .sr-title {
    font-size: 18px
    font-weight: 400
    color: var(--ink)
    line-height: 1.2
    margin-bottom: 5px
    white-space: nowrap
    overflow: hidden
    text-overflow: ellipsis
  }

  .sr-meta {
    display: flex
    align-items: center
    gap: 8px
  }

  .sr-sender {
    font-size: 12px
    color: var(--ink-mute)
    font-weight: 500
  }

  .sr-arrow {
    font-size: 15px
    flex-shrink: 0
    transition: none !important
  }

  .sr-arrow.dim {
    color: var(--ink-mute)
    opacity: 0.35
    animation: none
  }

  .sr-corner {
    position: absolute
    bottom: 8px
    right: 24px
    display: flex
    align-items: center
    gap: 5px
  }

  .sr-date-corner {
    font-size: 11px
    font-weight: 400
    color: var(--ink-mute)
    opacity: 0.7
  }

  .sr-corner-sep {
    font-size: 9px
    color: var(--ink-mute)
    opacity: 0.3
  }

  .sr-id {
    font-size: 9px
    font-weight: 600
    letter-spacing: 0.6px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.35
  }

══════════════════════════════════════════════════════════════════════
  ── 8. SHOW/HIDE TOGGLE
══════════════════════════════════════════════════════════════════════

  .show-more-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 10px 24px
    flex-shrink: 0
    border-top: 0.5px solid var(--divider)
  }

  .show-more-label {
    font-size: 13px
    font-weight: 600
    color: var(--ink-mute)
  }

  .show-more-label span {
    color: var(--ink-mute)
    opacity: 0.6
    font-size: 11px
    margin-left: 6px
  }

  .section-toggle {
    padding: 6px 0
    border-radius: 999px
    min-width: 64px
    text-align: center
    background: var(--base-whisper)
    border: none
    cursor: pointer
    font-size: 12px
    font-weight: 600
    color: var(--ink-mute)
    font-family: var(--font)
    transition: box-shadow 0.2s ease
    -webkit-tap-highlight-color: transparent
  }

  .section-toggle.collapsed {
    box-shadow: inset 1px 1px 3px var(--neu-dark), inset -1px -1px 3px var(--neu-light)
  }

  .section-toggle.expanded {
    box-shadow: 2px 2px 5px var(--neu-dark), -2px -2px 5px var(--neu-light)
  }

══════════════════════════════════════════════════════════════════════
  ── 10. ARCHIVED STATE
══════════════════════════════════════════════════════════════════════

  #sr-pane-archived .sr-item {
    opacity: 0.7
    pointer-events: none
    cursor: default
  }

  #sr-pane-archived .sr-title {
    color: var(--ink-mute)
    text-shadow: -1px -1px 1px var(--neu-dark), 1px 1px 1px var(--neu-light)
  }

  .light #sr-pane-archived .sr-title {
    text-shadow: -1px -1px 1px var(--neu-light), 1px 1px 1px var(--neu-dark)
  }

  #sr-pane-archived .sr-sender {
    opacity: 0.5
  }

  #sr-pane-archived .sr-date-corner {
    opacity: 0.4
  }

  #sr-pane-archived .sr-id {
    opacity: 0.2
  }

  #sr-pane-archived .sr-corner-sep {
    opacity: 0.2
  }

  #sr-pane-archived .show-more-label {
    opacity: 0.5
  }

══════════════════════════════════════════════════════════════════════
  ── 11. CTA BUTTONS
══════════════════════════════════════════════════════════════════════

  .sr-cta-wrap {
    padding: 24px 24px 32px
    flex-shrink: 0
  }

  .sr-cta-btn {
    width: 100%
    padding: 18px 24px
    border-radius: 999px
    background: var(--sedona)
    border: none
    cursor: pointer
    font-size: 16px
    font-weight: 700
    color: #fff7f3
    font-family: var(--font)
    box-shadow: var(--neu-sm)
    transition: transform 0.14s ease, box-shadow 0.14s ease
  }

  .sr-cta-btn:active {
    transform: scale(0.97)
    box-shadow: var(--neu-in)
  }

  .cta-btn {
    width: 100%
    padding: 16px 24px
    border-radius: 999px
    background: var(--sedona)
    border: none
    cursor: pointer
    font-size: 15px
    font-weight: 700
    color: #fff7f3
    font-family: var(--font)
    box-shadow: var(--neu-sm)
    transition: transform 0.12s ease, box-shadow 0.12s ease
    margin-top: 8px
  }

  .cta-btn:active {
    transform: scale(0.97)
    box-shadow: var(--neu-in)
  }

══════════════════════════════════════════════════════════════════════
  ── Drawer bottom buttons — accent
══════════════════════════════════════════════════════════════════════

  .cta-btn.accent {
    background: var(--accent)
    color: var(--base-mute)
  }

  .light .cta-btn.accent {
    color: var(--base-mute)
  }

══════════════════════════════════════════════════════════════════════
  ── 12. DRAWER SYSTEM
══════════════════════════════════════════════════════════════════════

  .drawer-scrim {
    position: absolute
    inset: 0
    z-index: 30
    background: var(--scrim)
    opacity: 0
    pointer-events: none
    backdrop-filter: blur(0px)
    -webkit-backdrop-filter: blur(0px)
    transition: opacity 0.46s ease, backdrop-filter 0.46s ease
  }

  .drawer-scrim.open {
    opacity: 1
    pointer-events: auto
  }

  .drawer-scrim.closing {
    opacity: 0
    transition: opacity 0.28s ease, backdrop-filter 0.28s ease
  }

  .drawer {
    position: absolute
    top: 12px
    right: 0
    bottom: 12px
    width: 88%
    z-index: 31
    background: var(--base)
    border-radius: 25px 0 0 25px
    display: flex
    flex-direction: column
    overflow: hidden
    will-change: transform
    transform: translateX(100%)
    transition: transform 0.46s cubic-bezier(0.16,1,0.3,1), background 0.3s ease
  }

  .drawer.open {
    transform: translateX(0)
  }

  .drawer.closing {
    transform: translateX(100%)
    transition: transform 0.28s cubic-bezier(0.4,0,1,1)
  }

  .drw-handle {
    display: flex
    justify-content: center
    padding: 12px 0 0
    flex-shrink: 0
  }

  .drw-handle-bar {
    width: 36px
    height: 4px
    border-radius: 2px
    background: var(--ink-mute)
    opacity: 0.3
  }

  .drw-nav {
    display: flex
    align-items: center
    gap: 12px
    padding: 16px 24px 0
    flex-shrink: 0
  }

  .drw-back {
    width: 32px
    height: 32px
    border-radius: 50%
    border: none
    cursor: pointer
    background: var(--base-whisper)
    display: flex
    align-items: center
    justify-content: center
    color: var(--ink-mute)
    font-size: 16px
    flex-shrink: 0
    box-shadow: 2px 2px 5px var(--neu-dark), -2px -2px 5px var(--neu-light)
    -webkit-tap-highlight-color: transparent
  }

  .drw-hint {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1.5px
    text-transform: uppercase
    color: var(--ink-mute)
  }

  .drw-body {
    flex: 1
    overflow-y: auto
    overflow-x: hidden
    scrollbar-width: none
    padding: 0 24px 0
  }

  .drw-body::-webkit-scrollbar {
    display: none
  }

══════════════════════════════════════════════════════════════════════
  ── 13. SR DETAIL DRAWER
══════════════════════════════════════════════════════════════════════

  .drw-photo-wrap {
    display: flex
    justify-content: center
    padding: 20px 0 18px
  }

  .drw-photo-circle {
    width: 110px
    height: 110px
    border-radius: 50%
    background: var(--base-whisper)
    box-shadow: inset 3px 3px 7px var(--neu-dark), inset -3px -3px 7px var(--neu-light)
    display: flex
    align-items: center
    justify-content: center
    flex-shrink: 0
  }

  .drw-photo-letter {
    font-size: 38px
    font-weight: 800
    color: var(--ink-mute)
    opacity: 0.2
    text-transform: uppercase
    letter-spacing: -1px
  }

  .drw-product-id {
    font-size: 10px
    font-weight: 600
    letter-spacing: 1px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
    margin-bottom: 4px
  }

  .drw-product-title {
    font-size: 26px
    font-weight: 700
    letter-spacing: -0.5px
    line-height: 1.1
    color: var(--ink)
    margin-bottom: 12px
  }

  .drw-status-row {
    margin-bottom: 16px
  }

  .status-pill {
    display: inline-flex
    align-items: center
    gap: 6px
    padding: 7px 14px
    border-radius: 999px
    font-size: 12px
    font-weight: 700
    letter-spacing: 0.5px
  }

  .status-pill.active-sr {
    background: var(--accent-mute)
    color: var(--accent)
  }

  .status-pill.pending {
    background: var(--base-whisper)
    color: var(--ink-mute)
  }

  .status-pill.in-progress {
    background: var(--progress-bg)
    color: var(--accent)
  }

  .status-pill.complete {
    background: var(--complete-bg)
    color: var(--complete-text)
  }

  .status-dot {
    width: 7px
    height: 7px
    border-radius: 50%
    background: currentColor
  }

  .drw-divider {
    height: 0.5px
    background: var(--divider)
    margin: 0 0 16px
  }

  .drw-message {
    font-size: 14px
    line-height: 1.65
    margin-bottom: 16px
  }

  .user-message {
    font-style: italic
    color: var(--ink-whisper)
  }

  .admin-message {
    font-style: normal
    color: var(--ink)
    padding-left: 12px
    border-left: 2px solid var(--accent)
  }

  .drw-show-more-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 4px 0 10px
  }

  .drw-show-more-label {
    font-size: 11px
    font-weight: 600
    letter-spacing: 0.5px
    color: var(--ink-mute)
    opacity: 0.6
  }

  .drw-more-overflow {
    display: none
  }

  .drw-more-overflow.open {
    display: block
  }

  .drw-details-row {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 4px 0 10px
  }

  .drw-details-label {
    font-size: 11px
    font-weight: 700
    letter-spacing: 1.4px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.5
  }

  .drw-details-body {
    display: none
    padding-bottom: 8px
  }

  .drw-details-body.open {
    display: block
  }

  .drw-detail-item {
    display: flex
    justify-content: space-between
    align-items: center
    padding: 10px 0
    border-bottom: 0.5px solid var(--divider)
  }

  .drw-detail-item:last-child {
    border-bottom: none
  }

  .drw-detail-key {
    font-size: 12px
    font-weight: 600
    letter-spacing: 0.5px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.6
  }

  .drw-detail-val {
    font-size: 14px
    font-weight: 500
    color: var(--ink)
  }

══════════════════════════════════════════════════════════════════════
  ── 14. NEW SR FORM DRAWER
══════════════════════════════════════════════════════════════════════

  .form-title {
    font-size: 22px
    font-weight: 700
    letter-spacing: -0.5px
    color: var(--ink)
    margin-bottom: 24px
    margin-top: 16px
  }

  .form-field {
    margin-bottom: 20px
  }

  .form-label {
    font-size: 10px
    font-weight: 700
    letter-spacing: 1.4px
    text-transform: uppercase
    color: var(--ink-mute)
    opacity: 0.6
    margin-bottom: 8px
    display: block
  }

  .form-input {
    width: 100%
    padding: 13px 16px
    border-radius: 12px
    border: none
    outline: none
    background: var(--base-whisper)
    color: var(--ink)
    font-family: var(--font)
    font-size: 14px
    box-shadow: inset 3px 3px 6px var(--neu-dark), inset -3px -3px 6px var(--neu-light)
    transition: box-shadow 0.14s ease
    resize: none
  }

  .form-input::placeholder {
    color: var(--ink-mute)
    opacity: 0.5
  }

  .form-input:focus {
    box-shadow: inset 4px 4px 9px var(--neu-dark), inset -3px -3px 7px var(--neu-light)
  }

  .form-textarea {
    min-height: 88px
  }

  .form-select {
    appearance: none
    -webkit-appearance: none
    cursor: pointer
  }

  .form-cta-wrap {
    padding: 8px 0 40px
  }

══════════════════════════════════════════════════════════════════════
  ── panel swipe to close
══════════════════════════════════════════════════════════════════════

  header.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); header.addEventListener('touchmove', onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── drawer swipe right to close
══════════════════════════════════════════════════════════════════════

  el.addEventListener('touchstart', onStart, {
    passive: true
  }

  ); el.addEventListener('touchmove', onMove, {
    passive: false
  }

══════════════════════════════════════════════════════════════════════
  ── Preview: add to top of locked list
══════════════════════════════════════════════════════════════════════

  if (drawerOpen) return; openDetailDrawer( {
    id: newItem.dataset.id, title: newItem.dataset.title, status: newItem.dataset.status, activity: newItem.dataset.activity, gem: newItem.dataset.gem, service: newItem.dataset.service, weight: newItem.dataset.weight, notes: newItem.dataset.notes, reply:
  }