the extract-css-files writes loc files from a txt file

the writelocs.py does the same thing but uses the dec and loc declarations first

use the output files from getdecs.py in data/audits/parameters



for extracring logic files must be inside of the _library fodler

to extract behavior fles must be inside the _library folder


extracting-css-blocks.py extracts all css blocks in a file

find-css blocks does the same





