scans decs and effs for duplicate raw variables and flags them.

the fix option removes all double decalartions from the every file in the dec directory and writes it at the top of the file, gives it i $_name_. then replaces each isntance within the file with $_name_

there is a recorvery option just runn script --restore

