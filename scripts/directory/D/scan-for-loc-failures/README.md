-  this is a sass command python scripts that prompts for a directory with scss files. It is meant to scan the locs directory and convert each scss into css. It outputs every error down the chain into the dec and eff files. Simpler, easier and much faster than building over and over again. 

**output direcotry:**

#  ----->   scripts/data/audits/sass    <----